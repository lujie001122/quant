# 撤单逻辑优化分析报告

## 一、现状分析

### 1.1 当前撤单流程

```
signal_generator.execute_signals()
  └─ _check_pending_orders()                    # 同步同花顺委托状态到本地 orders/
  └─ 遍历每个 actionable 信号:
       ├─ 检查是否需要撤单（pending_map / _is_duplicate_signal）
       ├─ 如果需要 → subprocess: python3 trade.py revoke CODE DIRECTION
       │    └─ trade.py do_revoke():
       │         ├─ 新 EvolvingSim 实例
       │         ├─ getEntrust('today', True)          # 查可撤委托
       │         ├─ 过滤: code + direction + 未成交
       │         └─ 逐笔: revokeEntrust(revokeType='contractNo', contractNo=contract)
       │              └─ _confirm_revoke(): getEntrust 验证 → 失败则重试一次
       └─ 执行新信号下单
```

### 1.2 核心痛点

| # | 痛点 | 详情 |
|---|------|------|
| 1 | **contractNo 撤单不可靠** | macOS 26 上 `revokeEntrust(revokeType='contractNo', contractNo=contract)` 报 `unknown err`，但 `revokeEntrust(revokeType='allBuyAndSell')` 正常工作 |
| 2 | **逐笔撤单效率低** | 每次撤单创建独立 EvolvingSim 实例 + 查委托 + 撤单 + 确认，有 N 笔委托就要 N 轮锁竞争 |
| 3 | **撤单粒度不匹配需求** | 用户需求是"无论标的无论方向，全部撤单"，但当前代码只撤"当前信号标的+方向"的旧单 |
| 4 | **信号间撤单不彻底** | 假设有 A 买入、B 卖出两个信号，A 的 revoke 只撤 A 的买入旧单，B 的买入旧单不会被撤——除非 B 恰好也是 actionable |
| 5 | **冗余的验证逻辑** | `_confirm_revoke` 再查一次 `getEntrust` 验证备注，增加 EvolvingSim 调用次数和锁竞争 |
| 6 | **subprocess 隔离开销** | `execute_signals` 通过 subprocess 调 `trade.py revoke`，每次 fork 进程 + import EvolvingSim，开销大 |

### 1.3 当前 `do_revoke` 中 contractNo 撤单的问题

```python
# trade.py:437 — 当前代码
result = e.revokeEntrust(revokeType='contractNo', contractNo=contract)
```

根据 skill 文档和用户反馈，`contractNo` 参数在 macOS 26 上报 `unknown err`。这是因为 EvolvingSim 底层的 AppleScript 命令构造对 `contractNo` 参数处理有问题，但 `allBuyAndSell` 参数走的是不同的 AppleScript 路径，正常工作。

---

## 二、优化方案

### 核心思路：**一次撤全部 → 重新挂全部**

```
信号来了 → revokeAllBuyAndSell（一次 EvolvingSim 调用）→ 遍历信号逐个挂单
```

### 2.1 trade.py 新增 `revoke_all` 函数

```python
def revoke_all():
    """
    撤销所有买卖委托（无论标的、无论方向）。
    使用 revokeEntrust(revokeType='allBuyAndSell') 一次调用完成。
    同花顺 24 小时可撤单，无需检查交易时段。
    """
    e = EvolvingSim()
    try:
        result = e.revokeEntrust(revokeType='allBuyAndSell')
        if result is not None:
            ok, info = result
            if ok:
                print(f"✅ 全部撤单成功")
            else:
                print(f"⚠️ 全部撤单返回: {info}（可能已无待撤委托）")
        else:
            print(f"⚠️ 全部撤单返回 None")

        # 将所有本地 pending 订单标记为 revoked
        _mark_all_pending_revoked()
        return True
    finally:
        time.sleep(2)


def _mark_all_pending_revoked():
    """将 orders/ 目录中所有 pending 状态订单标记为 revoked"""
    today = datetime.now().strftime('%Y%m%d')
    if not os.path.exists(ORDERS_DIR):
        return
    for fname in os.listdir(ORDERS_DIR):
        if not fname.startswith(today) or not fname.endswith('.json'):
            continue
        order_path = os.path.join(ORDERS_DIR, fname)
        try:
            with open(order_path, 'r') as f:
                order = json.load(f)
            if order.get('status') == 'pending':
                order['status'] = 'revoked'
                order['updated_at'] = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
                with open(order_path, 'w') as f:
                    json.dump(order, f, ensure_ascii=False, indent=2)
        except (IOError, json.JSONDecodeError):
            pass
```

### 2.2 signal_generator.py 简化 `execute_signals`

在遍历 actionable 信号**之前**，调一次 `revoke_all`：

```python
def execute_signals(result, mode=None):
    # ... 前面代码不变 ...

    # ═══ 全部撤单：信号来了，先清空所有挂单 ═══
    if actionable:
        print("[EXECUTE] 信号来了，先撤全部挂单...")
        subprocess.run(
            ["python3", trade_script, "revoke_all"],
            timeout=30, cwd=script_dir
        )
        # 撤完后重新加载订单状态
        pending_map, filled_codes = _check_pending_orders(mode=mode)

    # ═══ 遍历信号，直接挂单（不再需要 per-signal revoke） ═══
    for i, (code, sig) in enumerate(actionable):
        # 删除原有的 need_revoke 逻辑（约 50 行）
        # 直接进入下单流程
        ...
```

### 2.3 删除的代码

| 位置 | 删除内容 | 原因 |
|------|---------|------|
| `trade.py:403-482` | 整个 `do_revoke` 函数 | 不再需要逐笔撤单 |
| `trade.py:385-400` | `_confirm_revoke` 函数 | 不再需要验证撤单 |
| `signal_generator.py:978-1021` | `need_revoke` 检查 + 撤单 subprocess 调用 | 全部撤单已前置 |
| `signal_generator.py:621-638` | `_is_duplicate_signal` 函数 | 全部撤单后不再有重复信号问题 |
| `signal_generator.py:589-618` | `_check_pending_orders` 可简化 | pending_map 不再用于撤单决策 |

### 2.4 保留的代码

| 位置 | 保留内容 | 原因 |
|------|---------|------|
| `trade.py:185-211` | `_write_order` | 下单后仍需持久化 |
| `trade.py:214-235` | `_update_order_status` | 成交同步时仍需 |
| `signal_generator.py:522-586` | `_sync_entrust_to_orders` | 仍需同步同花顺状态到本地 |
| `signal_generator.py:495-510` | `_load_today_orders` | 仍需读取订单做去重判断 |
| `signal_generator.py:793-799` | `filled_codes` 跳过已成交 | 已成交不应重复挂单 |

---

## 三、方案对比

| 维度 | 当前方案 | 优化方案 |
|------|---------|---------|
| **EvolvingSim 调用次数** | N(信号) × (1 getEntrust + M(委托) × 2 revoke + M 确认) | 1 次 revoke_all |
| **锁竞争** | 每次 subprocess 新建实例，严重 | 无竞争 |
| **可靠性** | contractNo 报 unknown err | allBuyAndSell 已验证可用 |
| **代码行数** | ~120 行撤单逻辑 | ~30 行 |
| **覆盖范围** | 只撤当前信号标的+方向 | 全部标的全部方向 |
| **是否满足用户需求** | ❌ 不满足 | ✅ 完全满足 |
| **风险** | 逐笔撤单可能遗漏 | 全部撤单可能误撤不需要撤的挂单 |

### 风险分析

**全部撤单是否会误撤？**

用户需求明确："每次发信号，如果有挂单，无论标的无论方向，全部撤单，然后重新根据新信号挂单"。所以全部撤单不是风险，而是**需求本身**。

唯一需要注意的是：如果信号里没有某标的的操作，该标的的挂单也会被撤掉且不会重新挂。但这符合用户"新信号覆盖旧信号"的语义——没有新信号 = 不需要挂单。

---

## 四、实施步骤

### Step 1: trade.py 新增 `revoke_all`

在 `trade.py` 中新增 `revoke_all` 函数和 `_mark_all_pending_revoked` 辅助函数，在 `main` 中注册 `revoke_all` 命令。

### Step 2: 简化 `execute_signals`

在 `actionable` 列表非空时，前置调用一次 `python3 trade.py revoke_all`，然后删除 per-signal 的 `need_revoke` 逻辑块。

### Step 3: 删除 `do_revoke` 和 `_confirm_revoke`

这些函数不再被调用，可以删除以保持代码整洁。

### Step 4: 保留 `do_revoke` 为 CLI 备用

如果用户偶尔需要手动撤某个标的某个方向的单（非自动化场景），可以保留 `do_revoke` 但改为使用 `allBuyAndSell` 的降级方案：先撤全部，再重新挂不需要撤的。或者直接保留现有 `do_revoke` 作为手动工具，但主流程不再使用。

---

## 五、附加建议

### 5.1 同花顺激活前置

当前 `execute_signals` 中多次调用 `os.system("open -a 同花顺")` 和 `cliclick`。建议统一在 `revoke_all` 之前激活一次即可。

### 5.2 错误处理

`revokeEntrust(revokeType='allBuyAndSell')` 在无待撤委托时可能返回 `(False, ...)`，这不影响后续下单，只打 warning 即可。

### 5.3 保留 do_revoke 作为手动工具

```bash
python3 trade.py revoke CODE 买入    # 手动撤特定标的特定方向
python3 trade.py revoke_all           # 一键撤全部（自动化使用）
```

`do_revoke` 保留但改为内部也用 `allBuyAndSell` 降级（因为 contractNo 不可靠）。

### 5.4 batch 优化

如果未来信号量很大（>5个标的同时有信号），当前方案仍然是 O(1) 撤单 + O(N) 下单，已经是最优。