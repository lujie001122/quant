# 订单管理系统对交易逻辑影响 — 完整复盘报告

**日期**: 2026-08-13  
**范围**: `trade.py` / `signal_generator.py` / `orders/` 目录  
**方法**: 逐行阅读源码，验证文档与代码一致性

---

## 1. 订单文件 (orders/) 如何影响去重判断和信号执行

### 1.1 文件格式

`orders/{YYYYMMDD}_{code}_{action}.json`，action 取值 `buy/sell/t0_buy/t0_sell`，status 取值 `pending/filled/revoked/failed`。

### 1.2 写入端 (trade.py)

`_write_order()` 被以下函数调用，**每次写入都会覆盖同名文件**（同一 code+action 一天只有一份文件）：

| 调用方 | 时机 | 写入状态 |
|--------|------|---------|
| `_trade_with_confirmation` | EvolvingSim 返回 None | `failed` |
| `_trade_with_confirmation` | EvolvingSim 返回 (False, ...) | `failed` |
| `_trade_with_confirmation` | EvolvingSim 返回 (True, contract) | `pending` |
| `do_t0_buy` 配对单 | 主单 filled 后挂配对卖出单 | `pending` |
| `do_t0_sell` 配对单 | 主单 filled 后挂配对买入单 | `pending` |
| `revoke_all` | 全撤完成后 | pending→`revoked` |

**关键设计**：`_trade_with_confirmation` 不再做持仓增量确认，下单成功即写 `pending`，成交确认交给下次 cron 的 `getEntrust` 同步。这是"极简版 v6"设计，规避了 EvolvingSim 返回值不可信导致的重复下单 bug（2026-08-12 159611 被重复买入 5 次）。

### 1.3 读取端 (signal_generator.py)

- `_load_today_orders()`：读取 orders/ 下今日所有 JSON，返回 `{filename: order_dict}`
- `_check_pending_orders(mode)`：调用 `_sync_entrust_to_orders()` 同步同花顺状态后，返回 `(pending_map, filled_codes)`
- `execute_signals()` 中的去重：`if (code, sig_mode) in filled_codes: skip`

### 1.4 去重规则

**跨 cron 去重**（已实现）：
- 同 code + 同 mode（buy_sell/t0）+ 同花顺确认已成交 → 跳过
- mode 通过 `_trade_type_to_mode(trade_type)` 映射，买卖信号 → buy_sell，做T → t0

**同 cron 内跨信号去重**（未实现）：
- `_is_duplicate_signal` 函数在整个代码库中不存在（`search_files` 返回 0 匹配）
- 同一 cron 中，如果同一 code 有多个信号（理论上不会，`generate_signals` 每 code 只返回一个信号），会重复执行
- 文档声称存在但实际不存在，属于文档偏离

**实际风险**：低。`generate_signals()` 返回的 `signals` 字典以 code 为 key，同 code 只有一个信号条目。但做T信号和买卖信号同时在 `--execute=buy_sell` 模式下执行时，分别有独立的 trade_type（'t0' vs 'buy'），通过 mode 过滤正确隔离。

---

## 2. `_check_pending_orders` + `_sync_entrust_to_orders` 在 execute_signals 中的调用链路

### 2.1 完整调用链

```
signal_generator.py main()
  └─ generate_signals()          # 生成信号
  └─ execute_signals(result, mode)   # L679
       ├─ _check_pending_orders(mode)          # L758 ★ 已调用（非死代码）
       │    ├─ _sync_entrust_to_orders()       # L597: 同步同花顺实盘状态
       │    │    ├─ EvolvingSim().getEntrust('today', False)
       │    │    │    └─ 构建 ths_map: {code: {direction: status}}
       │    │    └─ 遍历本地 pending 订单，匹配 ths_map
       │    │         ├─ 同花顺"已成交/全部成交/部分成交" → 本地 filled
       │    │         └─ 同花顺"已撤单/已撤销/废单" → 本地 revoked
       │    └─ _load_today_orders()            # L599: 重新加载已更新订单
       │         └─ 构建 pending_map + filled_codes
       │
       ├─ 打印 filled_codes 去重信息           # L759-764
       ├─ subprocess.run trade.py revoke_all   # L770-787: 全撤
       │
       └─ for (code, sig) in actionable:       # L793
            ├─ sig_mode = _trade_type_to_mode(trade_type)
            ├─ if (code, sig_mode) in filled_codes: skip  # L799 ★ 去重
            └─ subprocess.run trade.py <action> ...       # L999
```

### 2.2 状态修正

**2026-08-13 审计文档声称 `_check_pending_orders` 是死代码**——此结论已过时。在 L758 明确调用：

```python
pending_map, filled_codes = _check_pending_orders(mode)
```

该调用在 2026-08-13 的某次修复中已加入（skill 文档 "execute_signals 流程（2026-08-13 修复）" 所述）。审计文档写于修复前。

### 2.3 `_sync_entrust_to_orders` 匹配粒度问题（P2 风险）

**当前实现**：按 `{code: {direction: status}}` 构建 ths_map，同一标的多笔同方向委托时只保留最后一条的状态。

```python
# signal_generator.py L542-551
ths_map = {}
for row in ent['data']:
    code = row[2]
    direction = row[4]
    status = row[5]
    if code not in ths_map:
        ths_map[code] = {}
    ths_map[code][direction] = status  # ← 覆盖，只保留最后一条
```

**风险场景**：做T买入后配对卖出挂单。同一 code 有两笔方向不同的委托（买入+卖出），但同方向只有一笔。当前按 code+direction 匹配，卖出方向只有一笔委托不影响。但如果同一 code 有多笔同方向委托（如做T买入主单+做T买入配对单重试），只保留最后一条状态，可能错误匹配。

**实际影响**：低。当前系统同 code 同方向通常只有一笔委托。

---

## 3. revoke_all 标记 revoked 后订单文件的完整生命周期

### 3.1 当前实现（trade.py L384-431）

**2026-08-13 审计文档声称 `revoke_all` 直接 `os.remove` 删除文件**——此结论已过时。当前代码正确实现：

```python
def revoke_all():
    # 1. 调用 EvolvingSim 全撤所有买卖委托
    e = EvolvingSim()
    result = e.revokeEntrust(revokeType='allBuyAndSell')
    
    # 2. 标记今日 pending 订单为 revoked，保留 filled
    for fname in os.listdir(ORDERS_DIR):
        if fname.startswith(today) and fname.endswith('.json'):
            order = json.load(open(order_path))
            if order.get('status') == 'pending':
                order['status'] = 'revoked'      # ★ 标记而非删除
                order['updated_at'] = ...
                json.dump(order, f)
```

### 3.2 完整生命周期

```
订单文件创建
    │
    ├─ _trade_with_confirmation: status='pending'
    │       │
    │       ├─ 下次 cron: _sync_entrust_to_orders()
    │       │   ├─ 同花顺已成交 → status='filled'   [终点]
    │       │   └─ 同花顺已撤单 → status='revoked'   [终点]
    │       │
    │       └─ 下次 cron: revoke_all()
    │           └─ status='revoked'                  [终点]
    │
    ├─ _trade_with_confirmation: status='failed'     [终点]
    │
    └─ do_t0_buy/do_t0_sell 配对挂单: status='pending'
            └─ 同上 pending 路径
```

### 3.3 关键观察

- `filled` 订单**永远不会被 revoke_all 修改**（L420: `if order.get('status') == 'pending'`）
- `filled` 订单作为去重依据，在后续 cron 中持续生效
- `revoked` 订单不被 `filled_codes` 收集（L613-616 只收集 filled），不影响去重
- 订单文件**跨天自动失效**（`_load_today_orders` 只加载今日文件）

---

## 4. 是否存在因订单状态错误导致的重复下单/遗漏下单风险

### 4.1 重复下单风险

| 风险场景 | 严重程度 | 当前防护 | 风险等级 |
|---------|---------|---------|---------|
| EvolvingSim 返回 (False, None) 但实际已成交 | 高 | `_trade_with_confirmation` 写 pending，不重试；下次 cron 同步 filled | ⚠️ 中 |
| 同一 cron 内同 code 两个信号 | 低 | `generate_signals` 每 code 一个信号 | ✅ 低 |
| execute_signals 重试 3 次导致重复下单 | 高 | 重试逻辑在 `not_filled` 和 `tonghuashun_disconnect` 时 break 不重试 | ⚠️ 中 |
| 配对挂单与主单方向相同导致重复 | 低 | 配对单是反方向（t0_buy 配对 sell，t0_sell 配对 buy） | ✅ 低 |

**重点风险：execute_signals 重试逻辑**（L997-1076）

```python
for attempt in range(1, max_retries + 1):  # max_retries=3
    proc = subprocess.run(cmd, ...)
    err_type, err_detail = _detect_error_type(...)
    
    if err_type is None and proc.returncode == 0:
        success = True; break             # ✅ 成功，不重试
    
    elif err_type == "tonghuashun_disconnect":
        # 重试（同花顺断连，前次肯定没成交）
    
    elif err_type == "not_filled":
        break                              # ✅ 不重试，防止重复
    
    else:
        break                              # ✅ 其他错误不重试
```

**但有一个 gap**：`_detect_error_type` 的判断逻辑依赖于 trade.py 的 stdout/stderr 输出。如果 trade.py 的 EvolvingSim 调用返回了 `(True, contract)` 但实际未成交（挂单状态），trade.py 的 stdout 会输出 "已挂单"，returncode=0，`_detect_error_type` 返回 None → 判断为成功 → 不重试。这是正确的。

但如果 EvolvingSim 返回 `(False, None)` 但实际已成交（历史 bug），trade.py 会输出 "下单失败"，`_detect_error_type` 可能返回 "other" → break 不重试。但文件已写入 status='failed'，下次 cron 同步时无法发现实际成交（因为同花顺委托记录可能不显示已成交的秒单）。

**此风险的实际防护**：trade.py 当前版本信任 EvolvingSim 返回 `(True, ...)` 即写 pending，返回 `(False, ...)` 即写 failed。如果 EvolvingSim 返回 False 但实际成交，订单文件标记为 failed，下次 cron 不会通过 `_sync_entrust_to_orders` 发现（因为 failed 订单被跳过）。但 `_check_pending_orders` 只收集 filled 和 pending 订单，failed 不被收集。信号生成器下次 cron 会重新生成信号 → 再次尝试下单 → 如果同花顺已有持仓，`_get_position_shares` 会检测到，不会重复。

### 4.2 遗漏下单风险

| 风险场景 | 严重程度 | 分析 |
|---------|---------|------|
| 去重过度：同 code 不同 action 被错误跳过 | 中 | `(code, sig_mode)` 去重，buy 和 t0_buy 分别有独立 mode。✅ 不遗漏 |
| 已成交订单阻碍新信号 | 低 | filled 只跳过同 mode 同 code。新信号可能不同 action（如卖出 vs 之前买入）。去重只检查 mode，不检查 direction。❌ **存在问题** |
| revoke_all 失败导致旧挂单阻塞 | 中 | 失败只打 ⚠️ 不阻塞。但旧挂单未撤，新下单可能冲突 |

**遗漏风险 1：去重不区分方向**

```python
# _check_pending_orders L613-616
if status == 'filled':
    filled_codes.add((code, order_mode))  # 只存 (code, mode)，不存 direction
```

如果当天 159516 买入已成交 → `filled_codes = {('159516', 'buy_sell')}`。后续卖出信号（同 code 同 mode）会被跳过！**这是一个真实遗漏风险**。

**实际影响**：低。买卖信号通常不会在同一天内同时出现（策略逻辑决定）。但做T场景下，做T买入成交后，做T卖出信号可能被跳过（同 code 同 t0 mode）。

**遗漏风险 2：t0 mode 下买入/卖出不区分**

做T场景：做T买入（主单）成交后，`filled_codes = {('159516', 't0')}`。后续做T卖出信号 → `_trade_type_to_mode('t0')` = 't0' → `('159516', 't0') in filled_codes` → **跳过**！这意味着做T买入成交后，同一天的做T卖出信号会被错误跳过。

---

## 5. trade.py 和 signal_generator.py 之间订单状态的同步链路完整性

### 5.1 同步链路图

```
┌─────────────────────────────────────────────────────────────────┐
│                        signal_generator.py                       │
│                                                                   │
│  execute_signals()                                                │
│    │                                                              │
│    ├─[1] _check_pending_orders(mode)                             │
│    │      ├─ _sync_entrust_to_orders()  ← 读同花顺实盘委托       │
│    │      │      └─ EvolvingSim.getEntrust('today', False)       │
│    │      └─ _load_today_orders()  ← 读本地 orders/*.json        │
│    │                                                                  │
│    ├─[2] revoke_all (via subprocess)                                  │
│    │      └─ trade.py: EvolvingSim.revokeEntrust(allBuyAndSell)      │
│    │           └─ 标记 pending → revoked                              │
│    │                                                                  │
│    └─[3] for each signal:                                            │
│           ├─ 去重检查: (code, sig_mode) in filled_codes               │
│           └─ subprocess.run trade.py <action>                         │
│                │                                                      │
│                ▼                                                      │
│  ┌─────────────────────────────────────────────────────────────┐     │
│  │                       trade.py                               │     │
│  │                                                               │     │
│  │  do_buy / do_sell / do_t0_buy / do_t0_sell                   │     │
│  │    └─ _trade_with_confirmation()                             │     │
│  │         ├─ EvolvingSim.buy/sell(...)  ← 同花顺下单            │     │
│  │         └─ _write_order(status='pending'/'failed')           │     │
│  │              └─ 写 orders/{date}_{code}_{action}.json        │     │
│  └─────────────────────────────────────────────────────────────┘     │
│                                                                       │
│  [下次 cron]                                                          │
│    └─ _check_pending_orders → _sync_entrust_to_orders()              │
│         └─ pending→filled (同花顺确认成交)                            │
└───────────────────────────────────────────────────────────────────────┘
```

### 5.2 同步链路完整性评估

| 同步环节 | 状态 | 说明 |
|---------|------|------|
| trade.py → orders/ 文件 | ✅ 完整 | `_write_order` 在所有路径中调用 |
| orders/ 文件 → signal_generator 去重 | ✅ 完整 | `_load_today_orders` → `_check_pending_orders` |
| 同花顺实盘 → orders/ 文件 | ✅ 完整 | `_sync_entrust_to_orders` 将 pending→filled/revoked |
| revoke_all 标记 | ✅ 完整 | pending→revoked，保留 filled |
| 跨天订单过期 | ✅ 完整 | `_load_today_orders` 只加载今日文件 |

**同步延迟**：订单状态同步不是实时的。trade.py 下单后写 pending，下一次 cron（20分钟后）才通过 `_sync_entrust_to_orders` 同步为 filled。在这 20 分钟窗口内，如果手动触发 `--execute`，会再次执行 `_check_pending_orders` 同步，所以延迟在手动触发时缩短。

### 5.3 链路断点

**断点 1：`_check_pending_orders` 只在 `execute_signals` 开头调用一次**

`execute_signals` 遍历信号过程中，每执行一个信号（通过 subprocess 调用 trade.py），trade.py 会写新的订单文件。但 `execute_signals` 的 `filled_codes` 在开头构建后不再更新。**同一 cron 内，先执行的信号成交后，后续同 code 同 mode 信号仍会被执行**。

不过如前所述，`generate_signals` 每 code 只有一个信号，所以此断点实际影响低。

**断点 2：trade.py 执行结果不反馈给 signal_generator**

`execute_signals` 通过 subprocess 调用 trade.py，只检查 returncode 和 stdout/stderr，不解析 trade.py 写回的订单文件来更新 `filled_codes`。如果 trade.py 成功下单（写 pending），但 signal_generator 不知道，后续信号无法利用此信息。

---

## 6. 做T买卖信号与买卖信号在订单系统中的互不干扰性

### 6.1 设计意图

通过 `mode` 字段实现互不干扰：
- `buy` / `sell` / `liquidate` / `reduce` → mode = `buy_sell`
- `t0` → mode = `t0`

### 6.2 实现机制

**写入端**：
- `do_buy` / `do_sell` → `_write_order(action='buy'/'sell')` → 文件名 `{date}_{code}_buy.json` 或 `{date}_{code}_sell.json`
- `do_t0_buy` → `_write_order(action='t0_buy')` + 配对 `_write_order(action='t0_sell')`
- `do_t0_sell` → `_write_order(action='t0_sell')` + 配对 `_write_order(action='t0_buy')`

**读取端**：
- `_check_pending_orders(mode)` 中 `_load_today_orders` 加载所有订单
- 按 `action` 字段判断 mode：`'t0' if 't0_' in action else 'buy_sell'`
- 接收 `mode` 参数过滤：`if mode and order_mode != mode: continue`

**去重端**：
- `filled_codes` 是 `set of (code, mode)` 元组
- 信号去重：`sig_mode = _trade_type_to_mode(trade_type)`，检查 `(code, sig_mode) in filled_codes`

### 6.3 互不干扰性验证

| 场景 | 预期 | 实际 | 结论 |
|------|------|------|------|
| 做T买入成交后，买卖买入信号 | 不被跳过 | `('159516', 'buy_sell')` 不在 filled_codes | ✅ 互不干扰 |
| 买卖买入成交后，做T买入信号 | 不被跳过 | `('159516', 't0')` 不在 filled_codes | ✅ 互不干扰 |
| 做T买入成交后，做T卖出信号 | 不应被跳过 | `('159516', 't0')` 在 filled_codes → **跳过** | ❌ 误判 |
| 买卖买入成交后，买卖卖出信号 | 不应被跳过 | `('159516', 'buy_sell')` 在 filled_codes → **跳过** | ❌ 误判 |

### 6.4 核心问题：去重 key 缺少 direction 维度

当前 `filled_codes` 使用 `(code, mode)` 二元组，缺少 `direction` 维度。这导致：

1. **做T场景**：做T买入成交 → 同 code 做T卖出被跳过（同 mode='t0'）
2. **买卖场景**：买入成交 → 同 code 卖出/清仓/减仓被跳过（同 mode='buy_sell'）

**建议修复**：`filled_codes` 改为 `(code, mode, direction)` 三元组，或使用 `(code, action)` 精确匹配。

### 6.5 做T执行流程中的实际表现

`--execute=buy_sell` 模式下：
1. `execute_signals` 开头 `_check_pending_orders(mode='buy_sell')` ← 只收集 mode='buy_sell' 的订单
2. 但 `actionable` 包含做T信号（`BUY_SELL_TYPES` + `T0_TYPES`）
3. 做T信号去重时 `sig_mode='t0'`，检查 `('code', 't0') in filled_codes` ← filled_codes 没有 t0 条目！
4. 做T信号**永远不会被去重跳过**（因为 mode='buy_sell' 时 filled_codes 只有 buy_sell 条目）

这是一个设计上的不对称：`--execute=buy_sell` 模式执行做T信号，但去重用的 filled_codes 不包含 t0 模式。**做T信号在 buy_sell 模式下没有去重保护**。

---

## 7. 总结：风险矩阵

| # | 风险 | 严重程度 | 根因 | 触发条件 |
|---|------|---------|------|---------|
| 1 | filled_codes 缺少 direction，同 mode 反方向信号被跳过 | **P0** | `(code, mode)` 二元组不够精确 | 同 code 先买入成交后卖出（或反之） |
| 2 | `--execute=buy_sell` 模式下做T信号无去重保护 | **P1** | filled_codes 只收集当前 mode 的订单 | 连续两次 cron 都生成做T信号 |
| 3 | `_is_duplicate_signal` 未实现，文档偏离 | **P2** | 函数从未实现 | 理论上同 cron 同 code 多个信号 |
| 4 | `_sync_entrust_to_orders` 按 code+direction 匹配，可能错配 | **P2** | 字典覆盖，多笔委托只保留最后 | 同 code 同方向多笔委托 |
| 5 | EvolvingSim 返回 False 但实际成交，订单写 failed | **P2** | EvolvingSim 返回值不可信（历史 bug） | EvolvingSim 间歇性异常 |
| 6 | execute_signals 重试 3 次可能导致重复下单 | **P2** | 重试逻辑在特定错误类型下会重试 | 同花顺断连恢复后重试 |

### 7.1 已修复的问题（确认）

| 问题 | 状态 |
|------|------|
| `_check_pending_orders` 死代码 | ✅ 已在 L758 调用 |
| `revoke_all` 删除文件而非标记 | ✅ 已改为标记 pending→revoked |
| `_trade_with_confirmation` 不信任 EvolvingSim 返回值 | ✅ 已改为直接写 pending |

### 7.2 推荐修复（按优先级）

**P0**：`filled_codes` 加入 direction 维度
```python
# _check_pending_orders 中
filled_codes.add((code, order_mode, order.get('direction', '')))

# execute_signals 去重中
sig_direction = '买入' if is_buy_direction else '卖出'
if (code, sig_mode, sig_direction) in filled_codes:
    skip
```

**P1**：`--execute=buy_sell` 模式下也收集 t0 订单
```python
# execute_signals 中
if mode == 'buy_sell':
    pending_map, filled_codes = _check_pending_orders(mode=None)  # 收集全部
else:
    pending_map, filled_codes = _check_pending_orders(mode)
```

**P2**：实现 `_is_duplicate_signal` 或确保文档与实际一致

**P2**：`_sync_entrust_to_orders` 改为按 contract 编号匹配