# 年化收益优化 — 调参方案分析报告

**日期**: 2026-08-18  
**当前回测**: 2024.1-2026.6 / 总收益 +48.15% / 年化 17.05% / 回撤 12.51% / 夏普 1.06  
**对比**: 2026 年 7 个月 +31.86%（年化 ~60%）

---

## 一、核心发现：config.yaml 对回测结果无直接影响

### 1.1 回测引擎独立硬编码

`backtest_bt.py` 的 `ETFStrategy.next()` 方法（L378-549）包含**完全独立的交易逻辑**，不读取 `config.yaml`：

| 参数 | config.yaml 值 | backtest_bt.py 硬编码值 | 影响 |
|------|---------------|----------------------|------|
| 单只仓位上限 | `position_cap: 1.00` (100%) | `_buy()` L321: `min(..., 0.50)` (50%) | 🔴 最大暴露减半 |
| 底仓/活动仓比例 | `dead_ratio: 0.0/1.0` | L74: `DEAD_RATIO=0.6/0.4` | 🟡 趋势止盈量计算偏小 |
| MA20入场过滤 | 方案C已移除 | L515: `ma20_ok = (price > ma20_v)` | 🔴 2024熊市长时间空仓 |
| 试探建仓比例 | 60% (方案C) | L548: `0.60` ✅ 已对齐 | 🟢 一致 |
| 确认加仓比例 | 100% (方案C) | L567: `0.60` ❌ 未对齐 | 🔴 应为100% |
| 总资金/单只上限 | `max_per_etf: 220000` | L70: `MAX_PER_ETF=44000` (20%) | 🟡 声明但未被 `_buy` 使用 |

### 1.2 策略模块与回测的关系

- `strategies/rsi_macd.py` 从 `position_info.py` → `config.yaml` 读取参数 ✅
- 但 `backtest_bt.py` 的 `next()` 方法**不调用**策略模块做交易决策
- 策略模块仅用于 `_validate_signals()` 交叉验证（L584-687），**不改变实际交易**
- 回测注释（L32-37）明确记录了这一点："回测直接调用 strategies.rsi_macd... 简化路径"

### 1.3 结论

**config.yaml 的任何修改都不会改变回测结果**。回测行为完全由 `backtest_bt.py` L67-75 和 L318-568 的硬编码值决定。

---

## 二、2024-2025 年收益低的原因分析

### 2.1 收益分解

| 时段 | 累计收益 | 年化 | 市场环境 |
|------|---------|------|---------|
| 2024.1-2025.12 | ~12.4% | ~6% | 半导体/科技 ETF 震荡回调 |
| 2026.1-2026.6 | ~31.86% | ~60% | 科技 ETF 强势上涨 |

### 2.2 三大制约因素

#### 🔴 制约1：MA20 入场过滤 → 2024 年长时间空仓

```python
# backtest_bt.py L515
ma20_ok = (ma20_v and price > ma20_v)
```

- 2024年半导体 ETF（159516/588170）在 MA20 下方运行数月
- 每次反弹到 MA20 上方时，方案C 的试探建仓 60% 可抓住机会
- 但硬编码的 MA20 过滤阻止了大部分入场，系统大部分时间空仓

#### 🔴 制约2：50% 仓位硬上限 → 涨幅被稀释

```python
# backtest_bt.py L321
target_value = min(self.broker.getvalue() * pct, self.broker.getvalue() * 0.50)
```

- 即使信号触发 60% 试探建仓，实际最多买 50%
- 确认加仓信号触发 100%，实际还是最多 50%
- 5 只 ETF × 50% 上限 = 最多 2 只满仓，3 只无法建仓
- 2026 年强势行情中，仓位不足导致涨幅被稀释

#### 🔴 制约3：确认加仓 60% vs 方案C 的 100%

```python
# backtest_bt.py L567: 确认加仓 60%（未对齐方案C）
# 方案C 要求: 确认加仓 100%（策略文件 rsi_macd.py L286 已实现）
```

- 2026年强势行情中，确认加仓只加 60% 而非 100%
- 少加了 40% 仓位，直接损失这部分收益

### 2.3 2026 年为何表现好

- 科技 ETF 价格持续在 MA20 上方 → MA20 过滤不阻塞
- 趋势跟踪/突破入场通道频繁触发
- 移动止盈让利润奔跑
- 但仓位上限仍限制了爆发力（50% 硬上限）

---

## 三、三个调参方案

> ⚠️ **重要前提**：以下方案需要同步修改 `backtest_bt.py` 中的硬编码值才能生效。  
> 如果只改 `config.yaml`，回测结果**不会变化**。

### 方案A：激进满仓（年化目标 30%+）

**核心思路**：解除仓位上限，让每只 ETF 可以满仓，最大化趋势收益。

| 修改位置 | 参数 | 当前值 | 目标值 | 理由 |
|---------|------|--------|--------|------|
| backtest_bt.py L321 | `_buy` 硬上限 | `0.50` | `1.00` | 解除仓位上限 |
| backtest_bt.py L567 | 确认加仓 | `0.60` | `1.00` | 对齐方案C |
| backtest_bt.py L515 | MA20 过滤 | `ma20_ok` 必需 | 移除 | 方案C 已移除 |
| backtest_bt.py L74-75 | DEAD/ACTIVE | `0.6/0.4` | `0.0/1.0` | 对齐方案C |
| config.yaml | 无需改动 | — | — | 当前 config 已是方案C |

**风险**：回撤可能从 12.51% 升至 15-18%，单只 ETF 暴跌时影响更大。

---

### 方案B：趋势优化（年化目标 25%+）

**核心思路**：保留 MA20 过滤但降低仓位上限，兼顾攻守。

| 修改位置 | 参数 | 当前值 | 目标值 | 理由 |
|---------|------|--------|--------|------|
| backtest_bt.py L321 | `_buy` 硬上限 | `0.50` | `0.75` | 单只最多 75% |
| backtest_bt.py L567 | 确认加仓 | `0.60` | `0.80` | 适当提高 |
| backtest_bt.py L515 | MA20 过滤 | 保留 | 保留 | 控制回撤 |
| backtest_bt.py L74-75 | DEAD/ACTIVE | `0.6/0.4` | `0.2/0.8` | 更多活动仓 |
| config.yaml | dead_ratio | `0.0` | `0.2` | 同步 |
| config.yaml | active_ratio | `1.0` | `0.8` | 同步 |

**风险**：回撤预计 12-15%，比方案A保守。

---

### 方案C：高频建仓（年化目标 20%+）

**核心思路**：不改变仓位上限，提高建仓频率和捕捉机会的能力。

| 修改位置 | 参数 | 当前值 | 目标值 | 理由 |
|---------|------|--------|--------|------|
| backtest_bt.py L515 | MA20 过滤 | 必需 | 移除 | 2024熊市不空仓 |
| backtest_bt.py L522 | RSI抄底阈值 | `rsi_val <= 80` | `rsi_val <= 70` | 收紧避免假信号 |
| backtest_bt.py L542 | Test抄底RSI | `rsi_val < 35` | `rsi_val < 40` | 更积极抄底 |
| backtest_bt.py L548 | 试探建仓RSI | `rsi_val > 30` | `rsi_val > 25` | 降低门槛 |
| backtest_bt.py L73 | COOLDOWN_DAYS | `2` | `1` | 更快重新入场 |
| config.yaml | rsi_minimal | `30` | `25` | 同步 |
| config.yaml | test_cap_rsi | `35` | `40` | 同步 |
| config.yaml | cooldown_days | `2` | `1` | 同步 |

**风险**：更多假信号和频繁交易，回撤可能升至 13-15%。

---

## 四、方案对比

| 指标 | 当前 | 方案A | 方案B | 方案C |
|------|------|------|------|------|
| 年化目标 | 17% | 30%+ | 25%+ | 20%+ |
| 预期回撤 | 12.5% | 15-18% | 12-15% | 13-15% |
| 仓位上限 | 50% | 100% | 75% | 50% |
| MA20过滤 | 保留 | 移除 | 保留 | 移除 |
| 确认加仓 | 60% | 100% | 80% | 60% |
| 修改文件数 | — | 1个py | 1py+1yaml | 1py+1yaml |
| 极端风险 | 中 | 高 | 中 | 中 |

---

## 五、推荐方案及实施步骤

### 推荐：方案A（激进满仓）

理由：当前 config.yaml 已是方案C，但回测未同步。方案A 对齐后预期年化 30%+，回撤仍可控（参考方案C 2年回测：+71.51%/年化30.96%/回撤11.96%）。

### 实施步骤

1. **修改 `backtest_bt.py`**（4处硬编码）：
   - L321: `0.50` → `1.00`
   - L567: `0.60` → `1.00`
   - L515: 移除 `ma20_ok` 条件
   - L74-75: `DEAD_RATIO=0.0`, `ACTIVE_RATIO=1.0`

2. **不需要修改 config.yaml**（当前已是方案C配置）

3. **运行回测验证**：
   ```bash
   cd /Users/lujie/Documents/code/quant
   python3 backtest_bt.py --period 2y
   ```

4. **预期结果**：参考方案C 2年回测 +71.51%/年化 30.96%/回撤 11.96%/夏普 1.60

---

## 六、附录：代码硬编码不一致清单

| 文件 | 行号 | 硬编码值 | config.yaml 对应值 | 状态 |
|------|------|---------|-------------------|------|
| backtest_bt.py | 70 | `MAX_PER_ETF = 44000` | `max_per_etf: 220000` | ❌ 不一致 |
| backtest_bt.py | 74 | `DEAD_RATIO = 0.6` | `dead_ratio: 0.0` | ❌ 不一致 |
| backtest_bt.py | 75 | `ACTIVE_RATIO = 0.4` | `active_ratio: 1.0` | ❌ 不一致 |
| backtest_bt.py | 321 | `min(..., 0.50)` | `position_cap: 1.00` | ❌ 不一致 |
| backtest_bt.py | 515 | `ma20_ok` 必需 | 方案C已移除 | ❌ 不一致 |
| backtest_bt.py | 567 | 确认加仓 `0.60` | 方案C要求 `1.00` | ❌ 不一致 |
| risk_manager.py | 44 | `MAX_DAILY_LOSS_PCT = 0.05` | 方案C已取消 | ❌ 不一致 |
| backtest_bt.py | 548 | 试探建仓 `0.60` | 方案C要求 `0.60` | ✅ 一致 |
| backtest_bt.py | 429 | 止损 `0.80` (20%) | `avg_cost_pct: 0.20` | ✅ 一致 |
| backtest_bt.py | 435 | 止盈 `1.60` (60%) | `hard_take_profit: 0.60` | ✅ 一致 |
| backtest_bt.py | 456 | 硬止损 `0.75` (25%) | `hard_stop_pct: 0.25` | ✅ 一致 |

**教训**：方案C 实施时修改了 7 个文件中的 6 个，但 `backtest_bt.py` 的硬编码常量段（L67-75）和 `_buy()` 方法未被同步更新。回测表现远低于预期（年化 17% vs 预期的 31%），根本原因在此。