# 订单管理测试方案

> 项目路径：`/Users/lujie/Documents/code/quant/`
> 核心模块：`trade.py`（下单/撤单/订单持久化）、`signal_generator.py`（信号生成/执行/去重/同步）
> 测试目标：覆盖订单管理的 6 个关键场景，不改代码，只输出测试方案

---

## 系统架构快速回顾

### 订单文件命名规则
```
orders/{YYYYMMDD}_{code}_{action}.json
```
- `action` 取值：`buy` | `sell` | `t0_buy` | `t0_sell`
- 做T和普通订单使用不同的 action 后缀，天然避免文件名冲突

### 订单文件结构
```json
{
  "code": "159516",
  "action": "buy",
  "shares": 100,
  "price": 0.756,
  "contract": "6198796687",
  "status": "pending|filled|revoked|failed",
  "direction": "买入|卖出",
  "created_at": "2026-08-13 07:39:52",
  "updated_at": "2026-08-13 07:39:52"
}
```

### 关键函数数据流
```
execute_signals()
  ├─ _check_pending_orders(mode)  ← 同步 THS + 构建 filled_codes
  │   ├─ _sync_entrust_to_orders()  ← getEntrust('today') 同步
  │   ├─ _load_today_orders()
  │   └─ 返回 (pending_map, filled_codes)
  │       filled_codes = {(code, mode, direction), ...}
  ├─ revoke_all (subprocess)  ← 全撤 pending 订单
  ├─ for each signal:
  │   ├─ sig_mode = _trade_type_to_mode(trade_type)  → 'buy_sell' | 't0'
  │   ├─ sig_direction = _get_signal_direction(...)  → 'buy' | 'sell'
  │   └─ if (code, sig_mode, sig_direction) in filled_codes → SKIP
  └─ 执行 trade.py subprocess
```

### filled_codes 三元组
```
(code, mode, direction)
例: ('159516', 'buy_sell', 'buy')
    ('159516', 't0', 'buy')
    ('159516', 'buy_sell', 'sell')
```

### mode 映射
| trade_type | mode |
|-----------|------|
| buy, sell, liquidate, reduce | `buy_sell` |
| t0 | `t0` |

---

## 测试案例 1：同标的买入成交后，同一天卖出信号不被跳过

### 背景
旧版 `filled_codes = {(code, mode)}` 缺少 direction 维度，导致同 code 同 mode 的反方向信号被误跳过。新版已改为 `{(code, mode, direction)}` 三元组。

### 测试目标
验证 `filled_codes` 加入 direction 维度后，同标的买入成交不会阻止卖出信号执行。

### 前置条件
- 当日无任何订单文件
- 同花顺连接正常

### 测试步骤

**Step 1：模拟买入成交**
```bash
# 手动创建一笔已成交的买入订单
mkdir -p orders/
cat > orders/$(date +%Y%m%d)_159516_buy.json << 'EOF'
{
  "code": "159516",
  "action": "buy",
  "shares": 100,
  "price": 0.756,
  "contract": "TEST00001",
  "status": "filled",
  "direction": "买入",
  "created_at": "2026-08-13 10:00:00",
  "updated_at": "2026-08-13 10:00:05"
}
EOF
```

**Step 2：构造卖出信号并验证去重逻辑**
```python
# 在 Python 中模拟 _check_pending_orders 和 execute_signals 的去重逻辑
from signal_generator import _check_pending_orders, _trade_type_to_mode, _get_signal_direction

# 获取 filled_codes（mode=None 表示全部收集）
pending_map, filled_codes = _check_pending_orders(mode=None)

# 预期：filled_codes 包含 ('159516', 'buy_sell', '买入')
print("filled_codes:", filled_codes)

# 模拟一个卖出信号
code = '159516'
trade_type = 'sell'
action = '卖出'

sig_mode = _trade_type_to_mode(trade_type)  # → 'buy_sell'
sig_direction = _get_signal_direction(trade_type, action)  # → 'sell'

should_skip = (code, sig_mode, sig_direction) in filled_codes
# 预期: ('159516', 'buy_sell', 'sell') NOT in filled_codes → should_skip = False
```

**Step 3：验证端到端流程**
```bash
# 模拟：先买入成交，再触发卖出信号
# 信号生成器应输出卖出信号，且 execute_signals 不应跳过

python3 -c "
import json, os
from datetime import datetime

# 1. 写买入已成交订单
os.makedirs('orders', exist_ok=True)
today = datetime.now().strftime('%Y%m%d')
buy_order = {
    'code': '159516', 'action': 'buy', 'shares': 100, 'price': 0.756,
    'contract': 'TEST001', 'status': 'filled', 'direction': '买入',
    'created_at': f'{today[:4]}-{today[4:6]}-{today[6:]} 10:00:00',
    'updated_at': f'{today[:4]}-{today[4:6]}-{today[6:]} 10:00:05'
}
with open(f'orders/{today}_159516_buy.json', 'w') as f:
    json.dump(buy_order, f, ensure_ascii=False, indent=2)

# 2. 加载订单并检查去重
from signal_generator import _check_pending_orders, _trade_type_to_mode, _get_signal_direction
pending_map, filled_codes = _check_pending_orders(mode=None)

print('filled_codes:', filled_codes)
# 预期包含: ('159516', 'buy_sell', '买入')

# 3. 模拟卖出信号检查
sig_mode = _trade_type_to_mode('sell')      # 'buy_sell'
sig_direction = _get_signal_direction('sell', '卖出')  # 'sell'
should_skip = ('159516', sig_mode, sig_direction) in filled_codes
print(f'卖出信号应被跳过? {should_skip}')  # 预期: False

# 清理
os.remove(f'orders/{today}_159516_buy.json')
"
```

### 预期结果
| 检查项 | 预期 |
|--------|------|
| `filled_codes` 包含 `('159516', 'buy_sell', '买入')` | ✅ |
| `('159516', 'buy_sell', '卖出')` 不在 `filled_codes` 中 | ✅ |
| 卖出信号 NOT skipped | ✅ |

### 边界情况
- 买入成交后，同标的做T买入信号：`('159516', 't0', 'buy')` 不在 filled_codes → 不跳过 ✅
- 买入成交后，同标的做T卖出信号：`('159516', 't0', 'sell')` 不在 filled_codes → 不跳过 ✅

---

## 测试案例 2：buy_sell 模式下去重也覆盖做T信号

### 背景
`--execute=buy_sell` 模式同时执行买卖信号和做T信号。需要验证做T信号在该模式下也能正确去重，不会被重复执行。

### 测试目标
验证 `_check_pending_orders` 在 `mode='buy_sell'` 时，`filled_codes` 仍收集全部模式（包括 t0），使得做T信号也能被去重。

### 前置条件
- 当日有一笔已成交的做T买入订单（`action='t0_buy'`）

### 测试步骤

**Step 1：创建已成交的做T订单**
```bash
cat > orders/$(date +%Y%m%d)_159516_t0_buy.json << 'EOF'
{
  "code": "159516",
  "action": "t0_buy",
  "shares": 100,
  "price": 0.750,
  "contract": "TEST00002",
  "status": "filled",
  "direction": "买入",
  "created_at": "2026-08-13 10:30:00",
  "updated_at": "2026-08-13 10:30:05"
}
EOF
```

**Step 2：验证 buy_sell 模式下 filled_codes 包含 t0 条目**
```python
from signal_generator import _check_pending_orders

# mode='buy_sell' 时，pending_map 只收集 buy_sell 模式的 pending，
# 但 filled_codes 收集全部模式（代码 L624-626）
pending_map, filled_codes = _check_pending_orders(mode='buy_sell')

print("filled_codes:", filled_codes)
# 预期包含: ('159516', 't0', '买入')
```

**Step 3：验证做T信号去重**
```python
from signal_generator import _trade_type_to_mode, _get_signal_direction

# 模拟做T买入信号
sig_mode = _trade_type_to_mode('t0')           # 't0'
sig_direction = _get_signal_direction('t0', '买入')  # 'buy'
should_skip = ('159516', sig_mode, sig_direction) in filled_codes
print(f"做T买入信号应被跳过? {should_skip}")  # 预期: True
```

**Step 4：验证做T卖出信号不被误跳过（方向不同）**
```python
sig_direction_sell = _get_signal_direction('t0', '卖出')
should_skip_sell = ('159516', 't0', sig_direction_sell) in filled_codes
print(f"做T卖出信号应被跳过? {should_skip_sell}")  # 预期: False
```

### 预期结果
| 检查项 | 预期 |
|--------|------|
| `mode='buy_sell'` 时 `filled_codes` 包含 `('159516', 't0', '买入')` | ✅ |
| 做T买入信号被跳过（已成交） | ✅ |
| 做T卖出信号不被跳过（方向不同） | ✅ |
| 普通买入信号不被跳过（mode 不同） | ✅ |

### 关键代码路径
```python
# signal_generator.py L624-626
# filled_codes 不受 mode 过滤，全部收集（去重用）
if status == 'filled':
    filled_codes.add((code, order_mode, direction))
```

---

## 测试案例 3：同一天多次 cron 触发，已成交的不重复下单

### 背景
cron 可能多次触发 `execute_signals`。同一天内，已成交的订单应在后续 cron 中被跳过。

### 测试目标
验证 `execute_signals` 的去重逻辑在多次 cron 触发场景下正确工作。

### 前置条件
- 同花顺连接正常
- 当日无订单文件

### 测试步骤

**Step 1：第一次 cron 触发 — 执行买入**
```python
# 模拟第一次 cron 触发
# 1. 订单文件空 → filled_codes = {}
# 2. execute_signals 遍历信号，执行 159516 买入
# 3. trade.py 写 orders/20260813_159516_buy.json (status='pending')
# 4. 同花顺成交 → _sync_entrust_to_orders 将 status 更新为 'filled'
```

**Step 2：第二次 cron 触发 — 验证去重**
```python
# 模拟第二次 cron 触发（同一天）
# 1. _check_pending_orders(mode) 被调用
# 2. _sync_entrust_to_orders() → getEntrust 返回 '已成交'
# 3. 订单文件 status 从 pending → filled
# 4. filled_codes 包含 ('159516', 'buy_sell', '买入')
# 5. 信号去重：('159516', 'buy_sell', '买入') in filled_codes → True
# 6. 信号被跳过，不重复下单
```

**Step 3：手动验证测试**
```python
import json, os
from datetime import datetime

today = datetime.now().strftime('%Y%m%d')
os.makedirs('orders', exist_ok=True)

# 模拟第一次 cron 后的状态：订单已成交
order = {
    'code': '159516', 'action': 'buy', 'shares': 100, 'price': 0.756,
    'contract': 'TEST003', 'status': 'filled', 'direction': '买入',
    'created_at': f'{today[:4]}-{today[4:6]}-{today[6:]} 09:35:00',
    'updated_at': f'{today[:4]}-{today[4:6]}-{today[6:]} 09:35:05'
}
with open(f'orders/{today}_159516_buy.json', 'w') as f:
    json.dump(order, f, ensure_ascii=False, indent=2)

# 模拟第二次 cron
from signal_generator import _check_pending_orders, _trade_type_to_mode, _get_signal_direction
pending_map, filled_codes = _check_pending_orders(mode='buy_sell')

assert ('159516', 'buy_sell', '买入') in filled_codes, "filled_codes 应包含已成交订单"

sig_mode = _trade_type_to_mode('buy')
sig_direction = _get_signal_direction('buy', '买入')
should_skip = ('159516', sig_mode, sig_direction) in filled_codes
assert should_skip, "第二次 cron 应跳过已成交信号"

print("✅ 第二次 cron 正确跳过已成交订单")
```

### 预期结果
| 检查项 | 预期 |
|--------|------|
| 第一次 cron 正常执行买入 | ✅ |
| 第二次 cron 跳过相同信号 | ✅ |
| `filled_codes` 正确反映已成交状态 | ✅ |

### 边界情况
- 同一 cron 内多个信号，前一个成交后，后一个同 code+mode+direction 信号是否跳过？
  - 答：`execute_signals` 中 `filled_codes` 在循环开头构建后不再更新，所以同一 cron 内先成交的不影响后续信号。这是已知的设计权衡（见复盘报告 L280）。
- 同 code 不同 mode（buy_sell vs t0）不互相干扰 ✅
- 同 code 不同 direction 不互相干扰 ✅

---

## 测试案例 4：revoke_all 后 pending 变 revoked，filled 不受影响

### 背景
`revoke_all()` 调用 EvolvingSim.revokeEntrust 撤销所有委托，然后将本地 `pending` 订单标记为 `revoked`，`filled` 订单保留不动。

### 测试目标
验证 `revoke_all` 只影响 pending 订单，不影响 filled 订单。

### 前置条件
- 当日 orders/ 目录下存在：
  - 1 笔 filled 订单
  - 2 笔 pending 订单
  - 1 笔 failed 订单

### 测试步骤

**Step 1：准备测试订单文件**
```bash
TODAY=$(date +%Y%m%d)
mkdir -p orders/

# filled 订单（应不受影响）
cat > orders/${TODAY}_159516_buy.json << 'EOF'
{"code":"159516","action":"buy","shares":100,"price":0.756,"contract":"F001","status":"filled","direction":"买入","created_at":"2026-08-13 09:30:00","updated_at":"2026-08-13 09:30:05"}
EOF

# pending 订单 1 — 买入（应变为 revoked）
cat > orders/${TODAY}_515880_buy.json << 'EOF'
{"code":"515880","action":"buy","shares":100,"price":1.250,"contract":"P001","status":"pending","direction":"买入","created_at":"2026-08-13 09:35:00","updated_at":"2026-08-13 09:35:00"}
EOF

# pending 订单 2 — 卖出（应变为 revoked）
cat > orders/${TODAY}_515880_sell.json << 'EOF'
{"code":"515880","action":"sell","shares":100,"price":1.280,"contract":"P002","status":"pending","direction":"卖出","created_at":"2026-08-13 09:36:00","updated_at":"2026-08-13 09:36:00"}
EOF

# failed 订单（revoke_all 只检查 pending，failed 不受影响）
cat > orders/${TODAY}_588170_buy.json << 'EOF'
{"code":"588170","action":"buy","shares":100,"price":0.900,"contract":"X001","status":"failed","direction":"买入","created_at":"2026-08-13 09:32:00","updated_at":"2026-08-13 09:32:00"}
EOF
```

**Step 2：执行 revoke_all（仅测订单状态变更，不调 EvolvingSim）**
```python
import json, os
from datetime import datetime

today = datetime.now().strftime('%Y%m%d')
orders_dir = 'orders'

# 模拟 revoke_all 中标记 pending → revoked 的逻辑
updated = 0
for fname in os.listdir(orders_dir):
    if not (fname.startswith(today) and fname.endswith('.json')):
        continue
    order_path = os.path.join(orders_dir, fname)
    with open(order_path, 'r') as f:
        order = json.load(f)
    
    old_status = order.get('status')
    if order.get('status') == 'pending':
        order['status'] = 'revoked'
        order['updated_at'] = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
        with open(order_path, 'w') as f:
            json.dump(order, f, ensure_ascii=False, indent=2)
        updated += 1
        print(f"  {fname}: {old_status} → revoked")
    else:
        print(f"  {fname}: {old_status} (不变)")

print(f"\n已标记 {updated} 个 pending 订单为 revoked")
```

**Step 3：验证结果**
```python
# 验证所有订单状态
expected = {
    f'{today}_159516_buy.json': 'filled',    # 不受影响
    f'{today}_515880_buy.json': 'revoked',   # pending → revoked
    f'{today}_515880_sell.json': 'revoked',  # pending → revoked
    f'{today}_588170_buy.json': 'failed',    # 不受影响
}

for fname, expected_status in expected.items():
    path = os.path.join(orders_dir, fname)
    with open(path) as f:
        order = json.load(f)
    actual = order['status']
    status = '✅' if actual == expected_status else '❌'
    print(f"{status} {fname}: {actual} (预期: {expected_status})")
```

### 预期结果
| 订单文件 | 初始状态 | 预期状态 | 说明 |
|---------|---------|---------|------|
| `{today}_159516_buy.json` | filled | filled | 已成交不变 |
| `{today}_515880_buy.json` | pending | revoked | 挂单被撤 |
| `{today}_515880_sell.json` | pending | revoked | 挂单被撤 |
| `{today}_588170_buy.json` | failed | failed | 失败订单不变 |

### 关键代码路径
```python
# trade.py L417-421
if order.get('status') == 'pending':
    order['status'] = 'revoked'
    order['updated_at'] = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
```

---

## 测试案例 5：做T买入和普通买入在订单文件中不互相覆盖

### 背景
订单文件名格式：`{date}_{code}_{action}.json`。`action` 区分 `buy` / `t0_buy`，因此做T和普通订单使用不同文件名，天然不会覆盖。

### 测试目标
验证做T订单和普通订单使用不同的文件名，互不覆盖。

### 测试步骤

**Step 1：创建普通买入订单**
```bash
TODAY=$(date +%Y%m%d)
mkdir -p orders/

cat > orders/${TODAY}_159516_buy.json << 'EOF'
{"code":"159516","action":"buy","shares":200,"price":0.756,"contract":"B001","status":"filled","direction":"买入","created_at":"2026-08-13 10:00:00","updated_at":"2026-08-13 10:00:05"}
EOF
```

**Step 2：创建做T买入订单（不应覆盖上面）**
```bash
cat > orders/${TODAY}_159516_t0_buy.json << 'EOF'
{"code":"159516","action":"t0_buy","shares":100,"price":0.750,"contract":"T001","status":"filled","direction":"买入","created_at":"2026-08-13 10:30:00","updated_at":"2026-08-13 10:30:05"}
EOF
```

**Step 3：验证两个文件都存在且内容正确**
```python
import json, os
from datetime import datetime

today = datetime.now().strftime('%Y%m%d')
orders_dir = 'orders'

buy_path = os.path.join(orders_dir, f'{today}_159516_buy.json')
t0_buy_path = os.path.join(orders_dir, f'{today}_159516_t0_buy.json')

with open(buy_path) as f:
    buy = json.load(f)
with open(t0_buy_path) as f:
    t0_buy = json.load(f)

assert buy['action'] == 'buy', f"普通买入 action 应为 'buy', 实际: {buy['action']}"
assert buy['shares'] == 200, f"普通买入 shares 应为 200, 实际: {buy['shares']}"
assert t0_buy['action'] == 't0_buy', f"做T买入 action 应为 't0_buy', 实际: {t0_buy['action']}"
assert t0_buy['shares'] == 100, f"做T买入 shares 应为 100, 实际: {t0_buy['shares']}"

print("✅ 普通买入订单和做T买入订单文件互不覆盖")
print(f"   普通买入: {buy_path} → {buy['shares']}股")
print(f"   做T买入: {t0_buy_path} → {t0_buy['shares']}股")
```

**Step 4：验证 _load_today_orders 能同时加载两者**
```python
from signal_generator import _load_today_orders

orders = _load_today_orders()
actions = [o['action'] for o in orders.values()]

assert 'buy' in actions, "应包含普通买入订单"
assert 't0_buy' in actions, "应包含做T买入订单"
print(f"✅ _load_today_orders 加载了 {len(orders)} 个订单: {actions}")
```

**Step 5：验证做T卖出和普通卖出也不覆盖**
```bash
cat > orders/${TODAY}_159516_sell.json << 'EOF'
{"code":"159516","action":"sell","shares":200,"price":0.770,"contract":"S001","status":"filled","direction":"卖出","created_at":"2026-08-13 11:00:00","updated_at":"2026-08-13 11:00:05"}
EOF

cat > orders/${TODAY}_159516_t0_sell.json << 'EOF'
{"code":"159516","action":"t0_sell","shares":100,"price":0.765,"contract":"TS001","status":"filled","direction":"卖出","created_at":"2026-08-13 11:30:00","updated_at":"2026-08-13 11:30:05"}
EOF
```

### 预期结果
| 文件名 | action | 覆盖风险 |
|--------|--------|---------|
| `{today}_159516_buy.json` | `buy` | 无 |
| `{today}_159516_t0_buy.json` | `t0_buy` | 无 |
| `{today}_159516_sell.json` | `sell` | 无 |
| `{today}_159516_t0_sell.json` | `t0_sell` | 无 |

### 关键代码路径
```python
# trade.py L192
order_path = os.path.join(ORDERS_DIR, f'{today}_{code}_{action}.json')
# action ∈ {buy, sell, t0_buy, t0_sell} → 四种文件名互不冲突
```

---

## 测试案例 6：同花顺断连后恢复，getEntrust 同步状态

### 背景
`_sync_entrust_to_orders()` 通过 `EvolvingSim.getEntrust('today', False)` 获取同花顺真实委托状态，将本地 `pending` 订单同步为 `filled` 或 `revoked`。

当同花顺断连时，`execute_signals` 会重试 3 次；恢复后，下一次 cron 触发时 `_sync_entrust_to_orders` 会同步状态。

### 测试目标
验证同花顺断连恢复后，`getEntrust` 能正确同步本地订单状态。

### 前置条件
- 本地有一笔 `pending` 状态的订单
- 同花顺中该订单已成交（状态为 '已成交'）

### 测试步骤

**Step 1：模拟断连前状态 — 本地订单 pending**
```bash
TODAY=$(date +%Y%m%d)
mkdir -p orders/

cat > orders/${TODAY}_159516_buy.json << 'EOF'
{
  "code": "159516",
  "action": "buy",
  "shares": 100,
  "price": 0.756,
  "contract": "DC001",
  "status": "pending",
  "direction": "买入",
  "created_at": "2026-08-13 09:35:00",
  "updated_at": "2026-08-13 09:35:00"
}
EOF
```

**Step 2：模拟同花顺 getEntrust 返回（已成交）**
```python
# 模拟 getEntrust 返回的数据结构
# row[2]=code, row[4]=direction, row[5]=status
mock_entrust_data = {
    'status': True,
    'data': [
        # ... 其他字段，index 2=code, 4=direction, 5=status
        ['', '', '159516', '', '买入', '已成交', '', '', '', '', 'DC001'],
    ]
}
```

**Step 3：验证 _sync_entrust_to_orders 逻辑**
```python
import json, os
from datetime import datetime

today = datetime.now().strftime('%Y%m%d')
orders_dir = 'orders'

# 模拟 _sync_entrust_to_orders 的核心逻辑
# 构建 ths_map: {code: {direction: status}}
ths_map = {
    '159516': {'买入': '已成交'},
}

# 遍历本地 pending 订单
for fname in os.listdir(orders_dir):
    if not (fname.startswith(today) and fname.endswith('.json')):
        continue
    order_path = os.path.join(orders_dir, fname)
    with open(order_path) as f:
        order = json.load(f)
    
    if order.get('status') != 'pending':
        continue
    
    code = order.get('code', '')
    direction = order.get('direction', '')
    
    if code in ths_map and direction in ths_map[code]:
        ths_status = ths_map[code][direction]
        new_status = None
        if ths_status in ('已成交', '全部成交', '部分成交'):
            new_status = 'filled'
        elif ths_status in ('已撤单', '已撤销', '废单'):
            new_status = 'revoked'
        
        if new_status:
            order['status'] = new_status
            order['updated_at'] = datetime.now().strftime('%Y-%m-%d %H:%M:%S')
            with open(order_path, 'w') as f:
                json.dump(order, f, ensure_ascii=False, indent=2)
            print(f"✅ {fname}: pending → {new_status} (同花顺:{ths_status})")

# 验证
with open(os.path.join(orders_dir, f'{today}_159516_buy.json')) as f:
    synced = json.load(f)
assert synced['status'] == 'filled', f"同步后状态应为 filled, 实际: {synced['status']}"
print(f"✅ 同步完成: {synced['code']} {synced['direction']} → {synced['status']}")
```

**Step 4：验证断连场景的完整流程**
```
时序:
  T1: cron 触发 → execute_signals
  T2: _sync_entrust_to_orders() → EvolvingSim 连接失败 → 返回（不更新任何订单）
  T3: execute_signals 遍历信号 → 159516 买入信号
  T4: subprocess trade.py buy → 同花顺断连 → 重试3次 → 失败告警
  T5: 同花顺恢复连接
  T6: 下一次 cron 触发 → _sync_entrust_to_orders() → 成功 → 同步状态
```

**Step 5：验证 _sync_entrust_to_orders 异常处理**
```python
# 验证异常时不会崩溃
# _sync_entrust_to_orders 的异常处理：
#   1. EvolvingSim 调用失败 → 打印告警，return
#   2. getEntrust 返回异常 → 打印告警，return
#   3. 单个订单文件更新失败 → 打印告警，continue

# 这些都已通过 try/except 保护，不会影响 execute_signals 主流程
```

### 预期结果
| 场景 | 预期行为 |
|------|---------|
| 同花顺正常，本地 pending → 同花顺已成交 | pending → filled |
| 同花顺正常，本地 pending → 同花顺已撤单 | pending → revoked |
| 同花顺断连，getEntrust 失败 | 打印告警，不影响后续流程 |
| 同花顺恢复后下次 cron | 自动同步状态 |
| 同花顺中无对应委托 | 本地 pending 保持不变 |

### 关键代码路径
```python
# signal_generator.py L536-600
# _sync_entrust_to_orders()
#   - L543: EvolvingSim().getEntrust('today', False)
#   - L556-565: 构建 ths_map = {code: {direction: status}}
#   - L582-586: 映射同花顺状态 → new_status
#   - L587-597: 更新本地订单文件
```

---

## 测试用例汇总表

| # | 测试场景 | 核心验证点 | 风险等级 |
|---|---------|-----------|---------|
| 1 | 买入成交后，卖出信号不被跳过 | `filled_codes` 三元组 direction 维度 | P0 |
| 2 | buy_sell 模式下去重覆盖做T信号 | `filled_codes` 全模式收集 | P1 |
| 3 | 多次 cron 触发不重复下单 | 去重逻辑 + 状态同步 | P0 |
| 4 | revoke_all 只影响 pending | pending→revoked, filled 不变 | P1 |
| 5 | 做T与普通订单文件不覆盖 | 文件名 action 区分 | P2 |
| 6 | 同花顺断连恢复后状态同步 | getEntrust 同步逻辑 | P0 |

---

## 测试执行建议

### 运行环境
- macOS，项目路径 `/Users/lujie/Documents/code/quant/`
- 需要同花顺和 EvolvingSim 支持（部分测试可纯逻辑验证，不依赖真实连接）

### 可纯逻辑验证的测试（无需同花顺连接）
- 案例 1、2、3、4、5：可通过构造订单文件 + 调用 `_check_pending_orders` 等函数验证
- 案例 5：纯文件名检查，无需任何外部依赖

### 需要真实同花顺连接的测试
- 案例 6：需要 `getEntrust` 返回真实数据
- 案例 4：`revoke_all` 的 EvolvingSim 调用部分

### 执行顺序
1. 先执行纯逻辑测试（案例 1-5）
2. 在有同花顺连接的环境执行案例 4（完整流程）和案例 6

### 清理
每个测试后应清理 `orders/` 目录下的测试文件，避免影响后续测试：
```bash
rm -f orders/$(date +%Y%m%d)_*.json
```