# ETF量化交易系统 — 专家全项目复盘报告

> 复盘日期：2026-08-11  
> 审查范围：signal_generator.py (2197行), trade.py (745行), backtest_bt.py (772行), rotation.py (762行), sentiment_check.py (206行), intraday_t_once.py (265行), tracker.py (248行)

---

## 1. 策略逻辑漏洞

### 1.1 建仓通道重叠与冲突

**问题**：6个建仓通道使用 `if action == "持有"` 串行检查，但通道3（突破入场）和通道4（分批建仓）条件高度重叠。

**位置**：`signal_generator.py` L1320-1347

```
通道1: MACD金叉 + RSI≤80 + price>MA20 → 30%
通道3: MACD金叉 + price>MA20 + 20日新高 → 30%
通道4: MACD金叉/红柱放大 + RSI>40 + price>MA5 + price>MA20 → 30%
```

**风险**：通道1和通道4在 MACD金叉 + RSI 40-80 + price>MA20 区间同时满足，但通道1先执行。这意味着通道4的分批建仓逻辑（AO过滤、MA5回踩）被通道1跳过，导致建仓缺乏动量确认。**通道1应降级为通道4之后**。

### 1.2 补仓逻辑与回测不一致

**位置**：`signal_generator.py` L1356（实盘），`backtest_bt.py` L557（回测）

实盘 `add_count < 5`，回测 `add_count < 5`，但 skill 文档说已优化为 `add_count < 3`。**代码与文档不一致**。另：实盘补仓触发条件是 `dip < -0.03`（-3%），而回测是 `dip < -0.03`，一致。但回测在 L557 使用的是 `dip < -0.03`，skill 文档说优化为 `dip < -0.05`（-5%），但代码实际上仍是-3%。

### 1.3 确认加仓的AO过滤在回测中缺失

**位置**：`signal_generator.py` L1373-1378（实盘），`backtest_bt.py` L562-568（回测）

实盘在确认加仓时有 AO 动量过滤（`ao_rising`），回测中也有 AO 检查但逻辑不同 —— 回测检查 `ao_val <= ao_5ago` 时跳过，但实盘检查 `ao_now > ao_5ago`。**逻辑一致，但回测中 AO 过滤失败时没有像实盘一样记录 ma5_touch_count**（L572-574），这可能导致回测中建仓确认频率低于实盘。

### 1.4 分级止损的恢复条件过于宽松

**位置**：`signal_generator.py` L1043-1054

```
止损恢复条件：price > MA20 AND DIF > 0 AND MACD红柱
```

存在问题：
- **止损1级触发后**：减仓30%，但 `stop_level=1` 后如果当天价格就回到MA20上方且DIF>0，会立即重置 `stop_level=0`。这意味着止损1级可能只减仓一次就恢复，分级止损的"渐进式减仓"意图被削弱。
- **止损2级恢复**：L1051对 `stop_level=2` 有额外的恢复条件，但与 L1043 的通用恢复条件重复。如果价格恢复但 MACD尚未恶化到死叉/绿柱放大，stop_level=2 会被重置，止损3级（清仓）永远不会触发。

### 1.5 保本止盈与移动止盈的冲突

**位置**：`signal_generator.py` L996-1001（保本），L1011-1014（移动止盈）

保本止盈触发条件：浮盈≥10%→激活，回落至成本+2%清仓。但移动止盈在浮盈≥8%时也会激活（`reached_8pct=True`），在15%后设 `trailing_stop_price = peak_price * 0.95`。

**问题**：如果浮盈在10%-15%之间，保本止盈（成本+2%）和移动止盈8%（成本+5%）同时存在，但 `stop_actions` 优先级中保本止盈排在移动止盈前面（L1069）。这意味着当价格从12%回落到成本+2%时，保本止盈先触发，移动止盈的5%回撤线永远不会触发。

### 1.6 做T买入有成本价保护但做T卖出无

**位置**：`signal_generator.py` L1128-1134

做T买入无成本约束（合理，因为5分钟RSI超卖就应该买入摊低成本）。做T卖出有 `price > pos.avg_cost` 约束（L1128），但 `intraday_t_once.py` 中做T卖出**没有成本价保护**（L236-246），两个文件不一致。

---

## 2. 参数合理性

### 2.1 MACD 参数：日线 vs 5分钟

| 场景 | 参数 | 位置 |
|------|------|------|
| 日线 MACD | fast=12, slow=26, sig=9 | signal_generator.py L317, backtest_bt.py L148 |
| 5分钟 MACD | fast=5, slow=34, sig=5 | signal_generator.py L397 |
| intraday_t_once MACD | fast=12, slow=26, sig=9 | intraday_t_once.py L16 (import from signal_generator) |

**问题**：`intraday_t_once.py` 导入 `calc_macd` 时使用的是默认参数 (12,26,9)，这是**日线参数**，但该模块用于日内做T，应该使用 5分钟参数 (5,34,5)。L188 调用 `calc_macd(closes)` 没有传参，默认就是日线参数。

**影响**：`intraday_t_once.py` 的 MACD 信号与 `signal_generator.py` 的做T信号不一致，因为它们用不同的 MACD 参数。两个做T信号源互相矛盾。

### 2.2 RSI 参数

| 场景 | 参数 | 评估 |
|------|------|------|
| 日线 RSI | Wilder RSI(14) | 标准，合理 |
| 5分钟 RSI | Wilder RSI(14) | 14根5分钟K线=70分钟，覆盖偏短 |
| 做T买入 RSI | <45 | 定位超卖，合理 |
| 做T卖出 RSI | >55 | 定位超买，**偏保守** |

**建议**：ETF（尤其是宽基ETF）波动较小，RSI>55 的超买阈值可能过于敏感。在震荡市中，RSI频繁在55附近波动，导致做T卖出信号过多。建议测试 RSI>60 或 RSI>65 作为卖出阈值，减少假信号。

### 2.3 ATR 参数

**位置**：`signal_generator.py` L301（日线ATR）、L421（5分钟ATR）

做T配对挂单使用 `ATR × 1.1`（L1214、L1241），这个乘数来自5分钟ATR。5分钟ATR通常只有日线ATR的1/10~1/20，乘以1.1后配对价差仍然很小。如果 ETF 价格在 1.0 元附近，5分钟ATR 约 0.003-0.005，配对价差约 0.0033-0.0055，乘以1500股约5-8元，经常无法通过 10 元价差校验。

**建议**：将配对乘数从 1.1 提高到 1.5-2.0，或降低价差校验阈值从 10 元到 5 元。

### 2.4 网格间距

**位置**：`signal_generator.py` L448-450（`dynamic_spacing`）

```python
adjusted = 0.6 * base_spacing + 0.4 * atr_pct
```

base_spacing 默认 0.025（2.5%）或 0.03（3%）。以 588170（科创半导体ETF）为例，base_spacing=0.03，ATR≈0.02，调整后 spacing≈0.026。5档网格覆盖 13% 跌幅，对 ETF 来说偏窄。在趋势下跌中，5档网格容易全部触发后冻结。

**建议**：将 GRID_BUY_LEVELS 从 5 提高到 7，或增加 base_spacing 到 0.035-0.04。

### 2.5 止损参数

| 止损类型 | 阈值 | 评估 |
|---------|------|------|
| 均价止损 | 12% | ETF 正常波动范围 3-5%，12% 合理 |
| 硬止损 | 20%（峰值回撤） | 偏大，ETF 从高点回撤 20% 意味着趋势已严重恶化 |
| 分级止损减仓 | 30%×2次 | 渐进式，合理 |
| 连续破MA20天数 | 2天 | 偏短，可能被假突破触发 |

**建议**：硬止损从 20% 降低到 15%，连续破MA20 从 2 天增加到 3 天。

---

## 3. 风控有效性

### 3.1 多层止损的实际覆盖

现有止损层级：
1. 保本止盈（10%→成本+2%）
2. 均价止损（12%）
3. 硬止盈（30%）
4. 移动止盈（8%/15%）
5. 硬止损（20%峰值回撤）
6. 分级止损（MA20→DIF→MACD）

**覆盖分析**：
- 温和下跌（5-10%）：保本止盈（如果已激活）先触发，否则分级止损（连续2天破MA20）触发
- 中等下跌（10-15%）：均价止损触发
- 急速下跌（>15%）：硬止损（20%峰值回撤）触发，但需要从峰值跌20%，如果建仓后从未盈利，峰值=买入价，20%回撤=买入价×0.80，比均价止损12%还宽松

**漏洞**：如果建仓后立即下跌，`peak_price` 未被更新超过买入价，硬止损20%的触发线（`peak_price * 0.80`）实际上等于买入价×0.80，比均价止损12%更宽松。**均价止损12%会先触发，硬止损20%在快速下跌场景中等于无效**。

### 3.2 黑天鹅场景

**缺失的保护**：
- 没有单日最大亏损限制（如单日跌超 8% 强制清仓）
- 没有全市场熔断机制（如大盘跌超 5% 暂停所有买入）
- 没有波动率风控（如 VIX 飙升时降低仓位）

**位置**：`signal_generator.py` L1296-1301 有 `defense_weak` 市场环境过滤，但 DEFENSE_CODE 被设为 None（L41），该过滤实际**从未生效**。

### 3.3 仓位上限的真实覆盖

**位置**：`signal_generator.py` L1162-1163

```
_position_capped = pos.has_position and _position_ratio_val >= 0.50
```

仓位上限 50% 是相对于 `TOTAL_FUND=220000`，但 `TOTAL_FUND` 是硬编码常量，不会随账户实际资金变化。如果账户盈利到 300,000，50% 仓位上限仍是 110,000（基于220,000），实际占比只有 36.7%。**资金增长后仓位上限自动变宽松**。

---

## 4. 做T策略：震荡市 vs 趋势市

### 4.1 趋势过滤分析

**位置**：`signal_generator.py` L1106-1111

```
ma20_5min_slope > 0.001 → 禁T卖出
ma20_5min_slope < -0.001 → 禁T买入
```

- 斜率阈值 0.1% 是基于 5 分钟 MA20 的，5 分钟 K 线波动大，MA20 斜率在 0.1% 附近频繁切换
- 斜率计算使用 5 根 K 线的 MA20 变化（L427-429），仅覆盖 25 分钟，对趋势判断不够稳健
- 当斜率在 -0.001 到 +0.001 之间时，允许双向做T，这是"震荡市做T"的意图

**问题**：在缓慢上升趋势中（如 0.05% 斜率），虽未达到 0.1% 阈值，但趋势已经偏多。此时做T卖出可能卖飞。建议增加第二层趋势过滤：如日线 MACD 方向 + 5分钟 MACD 方向。

### 4.2 配对挂单成交率

根据 skill 文档记录，2026-08-07 回测中 7 只标的只有 2 只成交，5 只单边市 0 成交。MA20 斜率过滤加入后，趋势市不做T，但**震荡市中配对挂单的成交率仍然取决于价格反转幅度**：

- 配对价差 = 5分钟ATR × 1.1，通常只有 0.003-0.005 元
- 如果配对买入价过低（做T卖出后），价格可能无法回落到该价位
- 配对单没有时间限制，可能挂几天都无法成交

**建议**：配对挂单增加有效期（如当日有效），避免过期挂单占用资金/持仓。

### 4.3 做T频率控制

**位置**：`signal_generator.py` L516-518

```
ATR > 5% → 3次/天
正常 → 2次/天
```

这里的 ATR 是 `atr_pct`（L978），计算逻辑是 `(atr_5min or atr) / price`。但 5 分钟 ATR 不可能达到 5%（那意味着每5分钟波动5%，极其罕见），所以实际上**永远走正常路线（2次/天）**。3次/天的扩展永远无法触发。

**建议**：将 ATR 阈值从 5% 改为 1%（5分钟ATR/价格），或使用日线 ATR。

---

## 5. 回测与实盘的差异点

### 5.1 数据源差异

| 模块 | 数据源 | 位置 |
|------|--------|------|
| signal_generator（实盘） | 腾讯 qfq（1200条）+ 新浪实时 | L77, L150 |
| backtest_bt（回测） | 腾讯 qfq（1250条） | L56 |
| intraday_t_once | **新浪** 5分钟K线 | L172 |
| signal_generator 5分钟 | **腾讯** 5分钟K线 | L231 |

**关键差异**：`intraday_t_once.py` 使用新浪 5 分钟 K 线（L172），而 `signal_generator.py` 使用腾讯 5 分钟 K 线（L231）。两个数据源的 K 线时间戳、价格可能存在微小差异，导致 RSI/MACD 计算结果不同。做T信号在两个文件中可能出现分歧。

### 5.2 回测缺失的功能

| 功能 | 回测 | 实盘 |
|------|------|------|
| 做T/日内信号 | ❌ 不含 | ✅ 含 |
| 网格交易 | ❌ 不含 | ✅ 含 |
| 舆情过滤 | ❌ 不含 | ✅ 含 |
| 防御盾 | ❌ 不含 | ✅ 含（但 DEFENSE_CODE=None） |
| 保本止盈 | ✅ 含 | ✅ 含 |
| 趋势止盈/破MA5卖活动仓 | ✅ 含 | ✅ 含 |

### 5.3 回测参数差异

| 参数 | 回测 | 实盘 | 位置 |
|------|------|------|------|
| 补仓 add_count | <5 | <5 | bt L557, sg L1356 |
| 补仓 dip | <-3% | <-3% | bt L557, sg L1358 |
| 趋势止盈卖出比例 | 10% | 10% | bt L499, sg L1057 |
| 破MA5卖出比例 | 5% | 5% | bt L508, sg L1061 |
| 确认加仓比例 | 70% | 70% | bt L567, sg L1398 |

参数基本一致，但**回测中确认加仓后重置 `reached_8/reached_15/breakeven_activated`**（L570-571），实盘中确认加仓后也重置（L1393-1395），一致。

### 5.4 滑点与手续费

回测使用 `SLIPPAGE_PCT = 0.001`（0.1%）+ FEE=5元固定（L21-22），实盘无滑点模型。ETF 交易免印花税，佣金通常万分之一，5元固定手续费对 ETF 偏高（买入 1 万元 ETF 实际佣金约 1 元）。**回测手续费偏高，实际收益可能略高于回测**。

---

## 6. 代码层面优化

### 6.1 冗余与重复

**6.1.1 MACD 参数不一致**

- `signal_generator.py` `calc_macd`：SMA 初始化（L321-322）
- `backtest_bt.py` `MACDStatus`：SMA 初始化（L165-166）
- `rotation.py` `calc_macd`：EMA 初始化（L201-209）

**三个文件使用不同的 MACD 初始化方法**：signal_generator 和 backtest_bt 用 SMA 预热，rotation.py 用 EMA 从头计算。EMA 初始化更标准，SMA 预热在前几个 bar 有偏差。

**6.1.2 RSI 实现重复**

- `signal_generator.py` `calc_rsi_wilder`（L284-299）
- `backtest_bt.py` `WilderRSI`（L118-142）
- `rotation.py` `calc_rsi`（L174-193）

三个独立的 RSI 实现，逻辑相似但细节不同（如 rotation.py 的 `avg_loss == 0` 返回 100.0，signal_generator 也返回 100.0）。应统一到 `tracker.py` 或新建 `indicators.py`。

**6.1.3 ATR 实现重复**

- `signal_generator.py` `calc_atr`（L301-315）
- `rotation.py` `calc_atr`（L271-286）
- `trade.py` `_calc_atr`（复制品）

三份 ATR 代码，完全可合并。

**6.1.4 5分钟K线获取重复**

- `signal_generator.py` `fetch_klines_5min`（L215-272）
- `trade.py` `_fetch_klines_5min`（复制品）

完全相同的代码，trade.py 应导入 signal_generator 的版本。

### 6.2 效率问题

**6.2.1 每次 generate_signals 都重新获取所有日K线**

**位置**：`signal_generator.py` L883-886

```python
if all_klines is None:
    all_klines = {}
    for code in ETFS:
        all_klines[code] = fetch_klines_daily(code)
```

每次调用 `generate_signals()` 都会拉取 1200 条日K线（每只 ETF），cron 每 20 分钟运行一次，一天拉取 15 次 × 6 只 ETF = 90 次 API 调用。**应缓存到本地文件**，仅当日期变化时重新拉取。

**6.2.2 实时行情获取在主逻辑之外**

`execute_signals()` 中重新调用 `fetch_realtime_quotes()`（L1787），但 `generate_signals()` 中也调用了（L875）。同一次运行中，实时行情被获取了两次。

**6.2.3 MACD 计算效率**

`calc_macd` 使用 O(n) 的 SMA 初始化，回测中每个 bar 都重新计算完整的 MACD 序列。`backtest_bt.py` 的 `MACDStatus.next()` 使用递推更新（L187-195），但 `rotation.py` 的 `calc_macd` 每次从头计算 EMA，效率低。

### 6.3 可维护性

**6.3.1 硬编码散落各处**

- `TOTAL_FUND = 220000`：signal_generator.py L34, backtest_bt.py L22
- `MAX_PER_ETF = 44000`：backtest_bt.py L23
- `COOLDOWN_DAYS = 2`：signal_generator.py L42, backtest_bt.py L26
- `DEAD_RATIO = 0.6, ACTIVE_RATIO = 0.4`：signal_generator.py L35-36, backtest_bt.py L27-28

**建议**：统一到 `config.py` 或 `etf_pool.json`。

**6.3.2 策略描述与实际代码不一致**

`signal_generator.py` L1506-1507 的 `strategy_policy` 描述是"均价止损**10%**"，但实际代码 L1004 是 `avg_cost * 0.88`（即**12%**）。文档与代码不一致。

**6.3.3 空仓天数递增逻辑有 bug**

**位置**：`signal_generator.py` L1444-1448

```python
if not pos.has_position and pos.build_phase == 0:
    if pos.empty_days_date != today_str:
        pos.empty_days += 1
        pos.empty_days_date = today_str
```

`empty_days` 在每次 cron 运行时（同一天多次）只递增一次，这是正确的。但 `empty_days` 在 `reset_on_liquidate()` 中被重置为 0（L589），这意味着清仓后立即重新计数空仓天数。**通道2（趋势跟踪）需要 `empty_days > 10`**，这可能导致清仓后 10 天内无法重新入场，即便趋势已经反转。

---

## 7. 资金管理

### 7.1 仓位分配

**位置**：`signal_generator.py` L33-36

```
TOTAL_FUND = 220000
DEAD_RATIO = 0.6
ACTIVE_RATIO = 0.4
fund_per_etf = 44000
```

6 只 ETF × 44000 = 264,000 > 220,000。如果 6 只 ETF 全部满仓 50%（110,000），需要 660,000。**资金池远不足以支撑全部满仓**。

**实际资金使用率**：
- 建仓：30%（13,200）+ 补仓：15%（6,600）+ 确认加仓：70%（30,800）= 50,600/只
- 6 只 ETF 全部建仓：303,600 > 220,000
- 策略依赖"不是所有 ETF 同时触发建仓"的假设

### 7.2 资金利用率问题

**问题**：`TOTAL_FUND` 是硬编码常量，不随账户实际资金变化。如果账户盈利到 300,000，`_position_capped` 仍以 220,000 为基准计算 50% 上限，导致实际仓位上限从 110,000 降到 110,000/300,000=36.7%。**盈利越多，仓位越保守**。

**建议**：从 `portfolio.json` 的 `account.total_asset` 动态读取总资产。

### 7.3 做T资金分配

做T使用 `总持仓 × 30%`（L1215），但做T买入需要额外资金，做T卖出会释放资金。如果同时触发多只 ETF 的做T买入信号，可能超出可用资金。

**当前无资金检查**：做T买入前没有检查可用资金是否足够。

---

## 8. 数据源差异分析

### 8.1 腾讯 vs 新浪 vs akshare

| 数据源 | 用途 | 延迟 | 可靠性 |
|--------|------|------|--------|
| 腾讯 qfq | 日K线（前复权） | T+0 | 高，640条可用 |
| 腾讯 m5 | 5分钟K线 | 实时 | 高 |
| 新浪 | 实时行情 | 0.2s | 中，偶尔格式变化 |
| 新浪 | 5分钟K线（intraday_t_once） | 实时 | 中，可能与腾讯不一致 |
| akshare | 日K线（降级） | T+0 | 低，本机频繁 RemoteDisconnected |
| 东财 | 舆情 | T+0 | 中，HTML解析 |
| 同花顺 | 舆情 | T+0 | 中 |

### 8.2 关键差异

**8.2.1 复权差异**

腾讯 qfq 使用前复权，akshare 也支持 `adjust="qfq"`。但回测中如果用 `day` 端点（非复权），数据会偏低。**skill 文档已指出此问题**。

**8.2.2 5分钟K线数据源差异**

`intraday_t_once.py` 使用新浪 5分钟K线（L172），`signal_generator.py` 使用腾讯 5分钟K线（L231）。两个数据源的差异：
- 时间戳对齐：新浪和腾讯的 5 分钟 K 线时间戳可能差 1-2 分钟
- K 线数量：新浪用 `datalen=100`，腾讯用 `datalen=48`
- 价格精度：新浪返回字符串，腾讯返回浮点数

**影响**：两个做T信号源（intraday_t_once 和 signal_generator）基于不同的数据源，可能产生矛盾信号。

**8.2.3 实时行情格式依赖**

**位置**：`signal_generator.py` L90-115

新浪实时行情解析依赖固定的字段顺序（`parts[3]`=当前价，`parts[2]`=昨收），如果新浪 API 返回格式变化，解析会失败但不报错（`pass` on L118）。应增加字段数量校验。

---

## 总结：优先级排序

### P0（立即修复）
1. `intraday_t_once.py` 的 MACD 参数使用日线参数（12,26,9）而非 5 分钟参数（5,34,5）—— L188
2. 策略描述"均价止损10%"与实际代码 12% 不一致 —— L1506
3. `DEFENSE_CODE = None` 导致市场环境过滤永久失效 —— L41

### P1（本周修复）
4. 建仓通道1（RSI抄底）应在通道4（分批建仓）之后 —— L1320
5. 做T ATR 阈值 5% 永远无法触发 3 次/天 —— L517
6. 空仓天数 `empty_days` 在清仓后重置为 0，导致通道2 10 天内无法入场 —— L589
7. 做T配对乘数 1.1 偏小，价差校验经常失败 —— L1214

### P2（下个迭代）
8. 提取公共指标模块（eliminate 3 份 RSI/ATR/MACD 实现）
9. 日K线数据缓存（避免 cron 每次重拉 1200 条）
10. 动态 TOTAL_FUND（从 portfolio.json 读取）
11. 硬止损 20% 降至 15%
12. 5分钟K线数据源统一为腾讯

### P3（长期优化）
13. 回测加入做T/网格模拟
14. 黑天鹅风控（单日最大亏损、大盘熔断）
15. 配对挂单有效期
16. 增加波动率自适应仓位

---

*报告生成：2026-08-11 | 审查工具：代码全量审查 + 跨文件对照 | 不修改代码*