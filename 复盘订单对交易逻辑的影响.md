# 复盘：订单管理系统对交易逻辑的影响分析

**日期**: 2026-08-13  
**分析范围**: `trade.py` / `signal_generator.py` / `orders/` 目录 / 架构文档

---

## 一、架构总览

```
market_data.py → strategy.py → signal_generator.py → trade.py
                                                          ↓
                                                     orders/ 目录
                                                  (订单持久化/去重)
```

### 核心链路

```
signal_generator.execute_signals()
  ├─ 1. 获取实时行情 (fetch_realtime_quotes)
  ├─ 2. 全撤所有挂单 (python3 trade.py revoke_all)
  │      └─ trade.py revoke_all():
  │           ├─ EvolvingSim.revokeEntrust(revokeType='allBuyAndSell')
  │           └─ 删除 orders/ 下今日所有 .json 文件
  ├─ 3. 遍历 actionable 信号 → 逐个 subprocess 调 trade.py 下单
  │      └─ trade.py _trade_with_confirmation():
  │           ├─ EvolvingSim.buy/sell(code, shares, price)
  │           └─ _write_order() → orders/{date}_{code}_{action}.json
  └─ 4. 输出统计 (成功/失败/跳过)
```

---

## 二、排查结果

### 问题 1: `_is_duplicate_signal` 函数不存在 ❌ 严重

**文档描述** (order-management-system.md):
> `_is_duplicate_signal()` — 检查同 code+方向+模式是否已有订单（排除 revoked）

**实际情况**: 该函数**从未被实现**。`signal_generator.py` 中不存在 `_is_duplicate_signal` 的定义或调用。

**影响**:
- `execute_signals` 遍历信号时，没有任何去重检查
- 同一次 cron 调用中，如果同一标的出现两个 actionable 信号（如买入信号 + 做T买入信号），两个都会被依次执行，可能重复下单

**代码证据**: `signal_generator.py` 中定义的函数列表：
```python
['generate_signals', '_load_today_orders', '_trade_type_to_mode',
 '_sync_entrust_to_orders', '_check_pending_orders', '_detect_error_type',
 '_signal_direction', '_build_retry_cmd', 'execute_signals', 'main']
```
没有 `_is_duplicate_signal`。

---

### 问题 2: `execute_signals` 不调用 `_check_pending_orders` ❌ 严重

**文档描述** (order-management-system.md):
> `_check_pending_orders` 内置 getEntrust 同步：调用 EvolvingSim().getEntrust('today', False) 获取同花顺真实委托状态... 返回 (pending_map, filled_codes) 供 execute_signals 使用

**实际情况**: `execute_signals` **完全不调用** `_check_pending_orders`。该函数仅在自身定义处存在（第 589 行），没有被任何地方调用。

**代码证据**: `execute_signals` 函数体内只有一个 `_check_pending_orders` 出现——但那是 `_sync_entrust_to_orders` 内部的调用（被 `_check_pending_orders` 调用），不是 `execute_signals` 直接调用。

**影响**:
- 同花顺真实成交状态**从未在 execute_signals 中同步**
- filled_codes 从未被构建，已成交的订单信息无法用于信号过滤
- `_sync_entrust_to_orders` 只在 `_check_pending_orders` 内被调用，而 `_check_pending_orders` 又无人调用 → **整个同步链路是死代码**

---

### 问题 3: `revoke_all` 删除订单文件，导致去重信息丢失 ⚠️ 中等

**当前行为** (`trade.py` 第 409-421 行):
```python
# 删除今天所有 orders/ 订单文件
if os.path.exists(ORDERS_DIR):
    today = datetime.now().strftime('%Y%m%d')
    for fname in os.listdir(ORDERS_DIR):
        if fname.startswith(today) and fname.endswith('.json'):
            os.remove(os.path.join(ORDERS_DIR, fname))
```

**问题**: `revoke_all` 不仅撤销同花顺上的委托，还**直接删除本地订单文件**。这与文档描述的 `_mark_all_pending_revoked` 不一致（文档说标记为 revoked，但代码是直接删除）。

**影响**:
- 撤单后，所有已成交 (filled) 的订单记录也被删除
- 同一天内，如果 cron 多次触发（每 20 分钟一次），第一次执行后订单文件被清空，后续 cron 运行时 `_load_today_orders()` 返回空
- 如果第一次 cron 下单成功（pending），第二次 cron 触发 → revoke_all 删除 pending 订单文件 → 无法知道哪些已成交、哪些待撤

**优化方案** (`撤单逻辑优化分析.md` 建议) 差异:
- 文档建议：`_mark_all_pending_revoked()` — 将 pending 订单标记为 revoked，保留 filled 记录
- 当前实现：直接删除所有文件 — filled 和 revoked 记录全部丢失

---

### 问题 4: `_sync_entrust_to_orders` 按 code+direction 匹配，非 contract ⚠️ 低

**代码** (`signal_generator.py` 第 540-551 行):
```python
ths_map = {}                    # {code: {direction: status}}
for row in ent['data']:
    code = row[2]
    direction = row[4]          # 买入/卖出
    status = row[5]
    ths_map[code][direction] = status
```

**问题**: 如果同一标的+方向有**多笔委托**（如多次买入），`ths_map` 只保留最后一个委托的状态。同花顺返回的委托列表可能有多条，但本地只匹配到第一条 pending 订单。

**影响**:
- 如果某标的买入 2 次（第一次成交、第二次 pending），同花顺返回 2 条委托记录
- 本地匹配时，第一条 pending 可能被匹配到错误的委托状态
- 多笔委托的同步可能出现遗漏

---

### 问题 5: 订单残留不影响去重，但语义混乱 ⚠️ 低

**当前 orders/ 目录**:
```
20260812_159516_buy.json   → status: revoked
20260812_159516_sell.json  → status: revoked
20260812_159611_sell.json  → status: pending (contract=6198784314)
20260812_512170_sell.json  → status: pending (contract=null)
```

**分析**: 今天是 2026-08-13，`_load_today_orders()` 筛选 `fname.startswith('20260813')`，所以这些昨天的文件**不会被加载**，不影响今日去重。

**但仍存在问题**:
- `159611_sell` 状态为 `pending` 且 contract 有效 — 说明该订单从未被 revoke_all 清理
- `512170_sell` 状态为 `pending` 且 `contract: null` — 说明 `_trade_with_confirmation` 返回了 pending 状态但没有 contract（可能是 `_write_order` 被直接调用时传了 None）
- 这些文件会一直残留，不会自动清理

---

### 问题 6: 同花顺激活逻辑重复 ⚠️ 低

`execute_signals` 中每个信号执行前都调用:
```python
os.system("open -a 同花顺")
os.system("cliclick c:960,50")
```

同时 `trade.py` 的 `_call_evolving` 不需要额外激活（EvolvingSim 内部会处理）。重复激活可能导致窗口焦点切换，干扰 EvolvingSim 的 AppleScript 交互。

---

## 三、风险矩阵

| 风险 | 严重度 | 发生概率 | 影响 |
|------|--------|---------|------|
| 同一 cron 中同标的多个信号被执行，重复下单 | 🔴 高 | 中 | 重复买入/卖出 |
| `_is_duplicate_signal` 缺失，无跨信号去重 | 🔴 高 | 中 | 同一标的同方向可能执行多次 |
| `_check_pending_orders` 死代码，同花顺状态未同步 | 🔴 高 | 高 | 已成交订单无法识别，可能重复下单 |
| revoke_all 删除 filled 订单记录 | 🟡 中 | 高 | 去重信息丢失，无法判断"今天已成交" |
| _sync 按 code+direction 非 contract 匹配 | 🟡 中 | 低 | 多笔委托同步可能出错 |
| 订单文件残留 | 🟢 低 | 高 | 不影响功能，但目录变脏 |

---

## 四、重复下单/遗漏下单风险分析

### 4.1 重复下单场景

**场景 A: 同一 cron 多次触发同一标的**
```
cron 10:00 → 信号: 159516 买入 → execute_signals
  ├─ revoke_all (orders/ 清空)
  ├─ 159516 买入 → trade.py buy → _write_order(159516, buy, pending)
  └─ orders/ 有 20260813_159516_buy.json

cron 10:20 → 信号: 159516 买入 → execute_signals
  ├─ revoke_all (撤销同花顺挂单 + 删除 orders/ 文件)
  │   └─ 如果 10:00 的订单已成交，revokeEntrust 撤不掉
  │      但订单文件被删除，无法知道"已成交"
  ├─ 159516 买入 → trade.py buy → _write_order(159516, buy, pending)
  └─ ⚠️ 如果 10:00 已成交，这里又买入一次 → 重复持仓
```

**根本原因**: `revoke_all` 删除订单文件后，丢失了"今天已成交"的记录。

**场景 B: 同一 cron 中多信号触发同一标的**
```
execute_signals:
  ├─ revoke_all (orders/ 清空)
  ├─ 159516 买入 (交易信号) → trade.py buy → _write_order(159516, buy)
  ├─ 159516 做T买入 (T0信号) → trade.py t0_buy → _write_order(159516, t0_buy)
  └─ ⚠️ 没有 _is_duplicate_signal 检查，两个信号都执行
```

**根本原因**: `_is_duplicate_signal` 不存在，`execute_signals` 不检查重复。

### 4.2 遗漏下单场景

**场景 C: revoke_all 失败导致后续下单被跳过**
```
revoke_all 返回 False (EvolvingSim 异常)
  → 但 execute_signals 继续执行 (revoke_all 失败不中断)
  → 同花顺上仍有旧挂单
  → 新信号下单时，同花顺可能因已有挂单而拒绝
  → 信号被遗漏
```

**代码证据** (`signal_generator.py` 第 759 行):
```python
os.system(f"python3 {trade_script} revoke_all")
```
`os.system` 的返回值**没有被检查**，revoke_all 失败也不影响后续流程。

---

## 五、总结

### 核心问题

1. **`_is_duplicate_signal` 缺失** — 文档声称存在，代码中不存在
2. **`_check_pending_orders` 死代码** — 无人调用，同花顺状态同步链路断裂
3. **`revoke_all` 删除 filled 订单** — 与文档建议的 `_mark_all_pending_revoked` 不一致
4. **`execute_signals` 无去重检查** — 遍历信号直接执行，不检查是否已成交

### 建议修复

1. **实现 `_is_duplicate_signal`** — 在 execute_signals 遍历信号前，检查同 code+方向+mode 是否已有订单
2. **在 execute_signals 开头调用 `_check_pending_orders`** — 同步同花顺委托状态，获取 filled_codes
3. **revoke_all 改为标记而非删除** — `_mark_all_pending_revoked()` 替代文件删除，保留 filled 记录
4. **检查 revoke_all 返回值** — 失败时打 [ALERT] 并决定是否继续
5. **清理残留订单文件** — 删除 20260812 的过期文件