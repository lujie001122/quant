# AI量化系统今日更新：小红书自动发帖 + 轮动舆情 + 交易复盘

## 小红书自动发布，终于搞定了

折腾了一晚上，`xiaohongshu-skill` 终于能用了。

踩的坑：
1. Playwright pipe 通信在 macOS 上超60秒就断（Node v22 bug），改用有头模式
2. "上传图文"按钮找到3个，strict mode 报错 → 取第一个
3. 按钮在 viewport 外面，Playwright click 失败 → 用 JS 原生 click
4. 有头和无头 Chromium 不共享 cookie → 统一用有头模式

现在一条命令就能发：
```bash
python3 -m scripts publish --title="标题" --content="正文" --images="图.png" --tags="标签" --headless=false --auto-publish
```

今晚这篇小红书就是自动发的，全程没碰鼠标。

## ETF轮动系统上线

每周一早上扫描20只ETF，选动量TOP3+防御2只替换池子。当前轮动池：

| 类型 | ETF | 近1月 | 
|------|-----|-------|
| 动量 | 酒ETF | +15.4% |
| 动量 | 共赢ETF | +12.6% |
| 动量 | 医疗ETF | +7.8% |
| 防御 | 红利ETF | +8.1% |
| 防御 | 电力ETF | +7.4% |

科技板块全线暴跌（-20%~-30%），轮动系统自动切到了防御方向。

## 舆情自动适配轮动池

`sentiment_check.py` 之前硬编码5个标的的关键词。现在从 `etf_pool.json` 动态读取，换池子不用手改代码。

今天舆情：全板块无利空，正常交易。

## 今日交易复盘

**清仓止损**：半导体设备ETF 浮亏-7.9%、科创半导体浮亏-3.2%，触发均价止损全清。

**当前持仓**：中证2000ETF 58600股，微盈0.12%。总资产20.7万，可用11.9万，仓位42%。

**做T优化**：量比之前是死代码（参数传None），现在用5分钟volume实际计算；RSI从硬门槛改为软评分；买入卖出冷却期分离，同方向30分钟不重复。

## 代码在GitHub

[github.com/lujie001122/quant](https://github.com/lujie001122/quant)

*个人量化笔记，不构成投资建议*